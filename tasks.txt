###
RECALL SCORE - DEVELOPMENT TASK CATALOGUE
Derived from "Product Definition Document.md"   |   Created 2026-08-04
###

HOW TO USE THIS FILE
  - One line per task. Mark progress by changing [ ] to [x], or [~] for
    in progress. Add notes under a task with a "  > " prefix.
  - "Ref N" points at the numbered functional requirements table in the
    Product Definition Document; "NFR-N" at the non-functional table.
    Go and read the acceptance criteria there before starting a task.
  - Near-term phases (A-C) carry detail. Later phases are deliberately one
    line each - their detail depends on decisions not yet made. Expand them
    when we get there.
  - This file is tracked in git (since 2026-08-05) so it is backed up.
    Commit it along with the code changes each task produces.
  - Completed tasks' notes were spring-cleaned 2026-08-07 down to essential
    outcomes and hard-won gotchas (root causes, non-obvious library
    behaviour) - file-by-file mechanism, test names/counts, and anything
    else recoverable via `git show`/`git log` on the task's own commit were
    cut. Unstarted/partial tasks were left untouched.

PHASE ORDER RATIONALE
  Phase A comes first because four defects in the timeline model gate large
  parts of the spec: notes are not attributed to their real part (blocks
  Ref 7), rests are discarded (blocks Refs 14 and 16), and divisions and
  time signature are read once from the first match (breaks Ref 18 on any
  score that changes either mid-piece). Building playback or filtering on
  top of these means reworking that code later.

  A2 lands before A3-A5 on purpose. Those three all rewrite
  _build_timeline_from_xml, and Refs 17 and 18 are correct today. The
  characterisation tests are what prove they are still correct afterwards.

CURRENT STATE
  This block described the pre-Phase-A state (2026-08-04) and had gone stale
  - everything it called "Broken"/"Partial" was resolved by Phase A-C and no
  longer reflected reality, which is itself the argument for not maintaining
  a second status summary by hand. As of 2026-08-10: Phases A-F are done
  (F1-F4 all [x]). The [x]/[ ] markers on each task below plus the
  COVERAGE CHECK table at the end of this file (Ref N -> owning task(s))
  are the authoritative status - check those instead of trusting a prose
  summary to stay in sync.

TEST SCORES (files/)
  Use these real MuseScore exports for functional/integration testing - each
  exercises a different set of requirements no hand-written fixture covers
  on its own. See tests/conftest.py for the score_* fixtures that expose
  them (score_bourree, score_duet, score_way_to_go, score_long_tune).
  bach-bourree-tab short  4/4, guitar, notation staff + TAB staff duplicating
                          it, pickup exported as number="1"
  Chessel Duet            6/8, TWO parts (Piano GM 1 with 2 staves + voices
                          1/5/6, Classical Guitar GM 25 with voices 1/2),
                          pickup exported as number="0" implicit="yes",
                          tempo eighth=96. Exercises multi-part, compound
                          time and the second pickup convention - it is
                          currently mis-parsed in several ways.
  Way To Go               4/4, TWO parts (Flute GM 74, Viola GM 42), no
                          pickup. Time signature changes 4/4 -> 3/4 -> 4/4
                          and key signature changes mid-piece (fifths 1 -> 2
                          -> 1). The score to use for A6 (divisions/time
                          signature tracked as they change, not first-match).
  long tune               4/4, one part, 130 measures on paper (real notes
                          end around measure 112 - measures 113-130 are
                          trailing empty bars padding the final system, the
                          same rest-only-tail pattern _sounding_bounds
                          excludes elsewhere, see C5). Provided by the user
                          for C4 (digit-entry bar jump, Ref 6), to exercise a
                          real multi-digit measure number end to end.
  Fingers and             4/4, TWO parts (Piano GM 1 with 2 staves, Acoustic
   articulations          Guitar GM 26 with 1 staff), no pickup. Real
                          MuseScore 4.7.4 export provided by the user for F3
                          (Ref 16 AC3/Ref 20). No score_* pytest fixture
                          wired up yet (checked directly via
                          MusicData(file_path=...) in an ad-hoc script when
                          F3 was live-tested) - add one the same way as the
                          four scores above if a test needs it directly;
                          F3's own regression tests instead use the smaller
                          hand-written dynamics_articulation_fingering.
                          musicxml/multi_value_technical.musicxml fixtures
                          (D-8 pattern). Exercises: a <direction>-based
                          dynamics mark (fff, then pp later) that must attach
                          to the note at its own offset/staff and not leak
                          onto a bass note sharing that offset; piano
                          fingering on both staves; guitar staccato/stress
                          articulations and a trill; and - the case that
                          caught F3's real bug - one guitar note whose
                          notations/technical carries THREE <pluck> marks
                          together (p/i/m/a rasgueado), which a naive
                          .find() silently truncated to just the first.
                          NOTE: the user intended to also add a thumb ("p")
                          pluck mark but it does not actually appear
                          anywhere in this export (checked via full-file
                          grep) - likely picked MuseScore's Dynamics "p"
                          palette item instead of the RH-fingering "p" one,
                          both being single-letter marks. Not a parser gap;
                          re-check if the user re-adds it and re-exports.


###
PHASE A - FOUNDATIONS AND MULTI-PART CORRECTNESS         (10 tasks)
###
[x] A1  Test harness: pytest + pytest-qt, tests/ tree, requirements files
        - NFR-03 AC-03.3
        > DONE 2026-08-05. Offscreen Qt, audio blocked via injectable synth
          (D-7), requirements split runtime/dev (mido dropped - unused since
          the FluidSynth switch). MusicData(file_path=...) tests bypass
          music21 for speed (~1ms vs ~460ms via MusicXMLReader.load()) - see
          CLAUDE.md for the full harness description.

[x] A2  Characterisation tests for the current parser - NFR-03, locks Ref 18
        > DONE 2026-08-05. Locked in ts-relative duration units and chord/
          string-fret/slice-ordering behaviour before A3-A6 rewrote the
          timeline builder, so regressions would show up as test failures.
          Recorded the known-bad pickup numbers as xfail, flipped in A3.

[x] A3  FIX pickup numbering when the file already numbers the pickup 0
        - Ref 17, currently broken
        > DONE 2026-08-06. Two MusicXML pickup conventions exist: number="1"
          (needs re-indexing down by one) and number="0" implicit="yes"
          (already correct - re-indexing it again produced measure -1 and
          corrupted beat positions). Fix only re-indexes when
          first_measure_number != 0.

[x] A4  Add part_id to NoteData; stamp each note with its real <part> id and
        that part's name - prerequisite for Ref 7
        > DONE 2026-08-06. Every note was labelled with parts_info[0]'s name,
          and (staff, voice) collided across parts with no part_id to
          disambiguate. NoteData/PartStructureInfo gained part_id; the
          timeline builder now stamps each note from its own <part>.

[x] A5  Explicit rests become timeline events - Ref 16 AC2
        > DONE 2026-08-06. NoteData.octave/midi_pitch are Optional[int]=None
          for a "rest" step. get_midi_notes_for_indices/
          get_region_4_data_for_indices guard on None so playback stays
          silent and Region 4 omits octave/midi rows. <forward>/<backup>
          gaps do NOT become events - only a literal <note><rest/>.

[x] A6  Track divisions and time signature as they change through the score
        instead of first-match - Ref 18
        > DONE 2026-08-06. .//attributes/time and .//attributes/divisions
          only ever matched the FIRST occurrence in the file. Replaced with
          apply_attributes(), re-applied every time an <attributes> element
          is walked (state resets per part - divisions is declared per-part
          in MusicXML), so ts_duration reflects the measure a note is
          actually in. Verified on Way To Go (4/4->3/4->4/4 mid-piece).

[x] A7  Measure index helpers over timeline_slices - prerequisite for Refs 3, 5, 6
        > DONE 2026-08-06. first_event_index_of_measure(n) returns None for
          an empty measure (used by C2/C4 for "unknown bar" handling instead
          of raising); last_event_index() returns -1 on an empty timeline.

[x] A8  FIX playback instrument per part/voice instead of always parts_info[0]
        - Ref 8, Ref 9 AC2, reported bug. Depends on A4.
        > DONE 2026-08-06. get_current_gmidi_program() always returned the
          first part's program (Chessel Duet's guitar played as piano) -
          removed; no single "current" instrument makes sense once a slice
          can hold notes from more than one part. D-5: one MIDI channel per
          part (part-list order, skip channel 10/percussion). SynthEngine
          gained play_chord(events, duration_ms) so a cross-part chord's
          channel groups start together instead of each group's
          stop_all_notes() cutting the previous one off - play_notes() is
          now a thin wrapper around it.

[x] A9  FIX tempo display so it reflects the time signature - reported bug
        > DONE 2026-08-06. music21's getQuarterBPM() always converts to
          quarter-note BPM (Chessel Duet, eighth=96, displayed "48 BPM").
          _extract_tempo now also returns a display_string built from the
          mark's own beat unit ("96 eighth notes per minute"); quarter_bpm
          is kept internally for playback timing, unaffected. UK/US wording
          deferred to F4/D-6.

[x] A10 Extract timeline building from MusicData into
        parsers/timeline_builder.py, one class per file - NFR-03 AC-03.1
        > DONE 2026-08-06. Moved the ElementTree walk out of MusicData
          verbatim (logic unchanged) into TimelineBuilder(file_path,
          parts_info).build(), called from __post_init__. No circular
          import - timeline_builder.py never imports models.music_data.


###
PHASE B - FINISH REGION 2 FILTERING AND UX REWRITE         (5 tasks)   Ref 7
###
[x] B1  MusicData.get_score_structure() emitting the parts_data shape that
        Region2HierarchyModel.build_from_score() documents - Ref 7
        > DONE 2026-08-06. Replaced get_region_2_data(), which built a flat
          dict keyed by f"Staff {s_id}" with no part id - two parts sharing
          a staff number silently overwrote each other's row. This was the
          root cause of the reported "viola has no stave in Region 2" bug on
          Way To Go; Region2HierarchyModel's node ids already included part
          id, so wiring up the real hierarchy fixed it as a side effect.

[x] B2  MusicData.set_active_voice_filter(); filter Region 3, Region 4 and
        playback by the active (part_id, staff, voice) tuples - Ref 7 AC1/AC2
        > DONE 2026-08-06. active_voice_filter: None means unfiltered (not
          "show nothing" - required since most of the existing suite builds
          MusicData without ever calling the setter). New _visible_notes()
          is the single filter point all four accessors read through, so a
          Region 3 row index means the same note everywhere even when some
          notes are hidden. Filtering changes what's shown/played at a
          position, not which positions Left/Right visits.

[x] B3  Remove the hasattr guards in main_window.py; confirm toggling works
        and timeline position is retained - Ref 7 AC3
        > DONE 2026-08-06. Guards removed - get_score_structure()/
          set_active_voice_filter() are unconditional calls now.

[x] B4  Test: toggling the TAB staff off removes the duplicate notes on the
        Bourree sample (first slice is E,G,E,G today) - Refs 7, 8
        > DONE 2026-08-06.

[x] B5  Region 2 UX rewrite: replace the two-column table with a flat list
        navigated by Up/Down only, toggled by O - added beyond the original
        B1-B4 scope after the table view was found "clumsy" and the
        Product Definition Document's Key Strokes table was updated to O
        - Ref 7
        > DONE 2026-08-06. New Region2ListWidget(QListWidget): each row's
          text embeds name/level/status in one line so NVDA reads the whole
          row on one Down-arrow instead of column-by-column table
          navigation, e.g. "Classical Guitar - on", "  Treble stave - on".
          RegionTableWidget stripped back to a plain property table for
          Regions 1/4 only. Confirmed toggling a parent off/on restores each
          child's own prior state (not a reset) - locked in with a test.


###
PHASE C - NAVIGATION AND WINDOW CHROME                    (8 tasks)
###
[x] C1  Confirm/lock in Note-region-scoped navigation - Ref 4, D-2 RESOLVED
        > DONE 2026-08-06. TimelineListWidget already intercepted Left/Right
          scoped to its own focus; confirmed the other three region widgets
          never call navigate_timeline_* and locked it in with a regression
          test.

[x] C2  Ctrl+Left / Ctrl+Right jump by measure - Ref 3
        > DONE 2026-08-06. Jumps are "sympathetic to active notes, not just
          what is in the score" per the user's clarification - skips
          measures left entirely invisible by the Region 2 voice filter.
          Pickup bar (measure 0) falls out for free since measure_numbers()
          already lists it first.

[x] C3  Home / End jump to first and last event - Ref 5
        > DONE 2026-08-06. Unlike Left/Right/Ctrl+Left/Right, Home/End never
          fail outward - no boundary cue call. No screen-reader announcement
          yet (D3).

[x] C4  Type digits then Enter to jump to a bar; Escape clears; unknown bar
        plays an error sound and does not move - Ref 6
        > DONE 2026-08-06. State (_pending_digits) lives in
          TimelineListWidget per D-2 (per-widget, not global).
          jump_to_measure() bounds against _sounding_bounds() so a bar
          that's only trailing rest padding isn't a valid target either. Any
          other handled key clears pending digits first, so stale typed
          digits can't silently wait for a future Enter.
        > FOLLOW-UP 2026-08-06: the user's real 'Long tune' score landed
          (files/long tune/, 130 measures on paper, notes end ~112, rest is
          trailing empty padding - same pattern C5 excludes). Added
          score_long_tune fixture, tested jump to measure 100.
        > FOLLOW-UP 2026-08-27 (user-requested): made the feature global,
          matching Play/Pause/Stop/Preview/tempo. The buffer moved from
          TimelineListWidget to NavigationController.pending_digits
          (reversing D-2's per-widget choice - a bar jump is a
          position-wide action, not a per-row one). main_window.setup_
          shortcuts binds bare 0-9 and Escape as WindowShortcuts; Enter is
          the Preview QAction routing through audition_phrase, which
          commits a pending number before previewing. Any cursor move
          (NavigationController) clears the buffer. FocusController's
          Note-region carve-out for the Preview action
          (update_preview_action_enabled) is gone - the action is simply
          always enabled now.

[x] C5  Audible boundary notification ("doh" sound) - Ref 2 AC4, Ref 3 AC4
        > DONE 2026-08-06. D-9: synthesized through the existing SynthEngine
          rather than a new asset - a short, quiet low note on a dedicated
          channel (15) so it can't collide with a part's own instrument.
        > FIX 2026-08-06 (live-tested bug): "active event" for boundary
          purposes included trailing rest-only padding at the end of the
          final bar (Chessel Duet's last bar has rests in every voice on
          beats 4/5/6 after the last real note) - Right/Ctrl+Right stepped
          through the rests before hitting the boundary, and End landed on
          an empty rest instead of the last real note. Fixed by introducing
          _sounding_bounds() (first/last index with a visible note whose
          midi_pitch is not None) as the real navigable range for
          Left/Right/Home/End/measure-jump - an interior rest sandwiched
          between sounding notes stays individually reachable; only
          leading/trailing rest-only padding is excluded.
        > Boundary cue tuned on request: C#2 at 100ms (was E2 at 200ms).

[x] C6  Status bar: measure/beat/key/time-signature at the current cursor
        position, backed by new per-event key+time-signature tracking -
        Functional Spec, resolves the "Status bar" Discussion Point
        > DONE 2026-08-06. Key signature (fifths) now tracked per-EventSlice
          the same way A6 tracks time signature (D-11) - FIFTHS_MAP moved to
          models/key_signatures.py so both Region 1 and the live status bar
          share one mapping without a parsers->models reverse import.
          StatusBarWidget uses QMainWindow.setStatusBar() (not a custom
          widget) so NVDA+End ("report status bar") works natively. Its 3
          fields cycle Tab/Shift+Tab among themselves only, independent of
          the regions' own cycle (see C7).

[x] C7  F6 / Shift+F6 toggle focus between the regions area (last-focused)
        and the status bar - Functional Spec
        > DONE 2026-08-06, revised twice after live testing - see D-10/D-12
          for the settled design. Gotchas found by testing, not assumption:
          1. QMenuBar has no focus policy by default - menuBar().setFocus()
             alone does nothing (moot once the pane count below dropped to
             two, but worth remembering for any future menu-bar focus work).
          2. Qt.ShortcutContext.ApplicationShortcut caused ambiguous-shortcut
             failures once more than one MainWindow existed in the same
             QApplication (true across sequential pytest tests, not just a
             hypothetical) - switched to WindowShortcut, which still fires
             regardless of which child widget within THIS window has focus.
          3. CORRECTION (live-tested, user reversed their own original
             3-pane spec after trying it): F6/Shift+F6 now toggle between
             just TWO panes (regions, status bar) - menu bar access stays on
             the OS's native Alt mechanism. See D-10.
        > FIX 2026-08-06 (live-tested, two rounds): Tab from Region 4 was
          landing on the status bar instead of wrapping to Region 1, and
          fixing that broke a different edge (Region 1 -> status bar instead
          of Region 2). ROOT CAUSE, confirmed by direct experiment:
          QWidget.setTabOrder(a, b) splices into ONE shared window-wide
          focus ring; relocating any node resets its own outgoing pointer as
          a side effect. Closing a 4-widget loop needs all four touched, and
          the dependency between the calls is circular - no call order
          satisfies it. This is a structural limitation of pairwise chain
          splicing applied to a closed ring, not a bug in how this code used
          setTabOrder - not fixable by reordering. Fixed by abandoning
          setTabOrder/focusNextChild/focusPreviousChild for the regions area
          entirely: MainWindow.focus_next_region/focus_previous_region index
          into a fixed 4-widget list and call .setFocus() directly (D-12) -
          same pattern the status bar's own Tab cycling already used.
          GOTCHA WORTH REMEMBERING: don't use setTabOrder to close a
          focus-cycle loop of 3+ widgets in this codebase.

[x] C8  Menu bar: add Navigation menu (First/Last measure, Goto Measure
        dialog) and Help menu (About); Edit/View/Options deferred to their
        owning phases - Functional Spec
        > DONE 2026-08-06. GotoMeasureDialog/AboutDialog are constructed by
          name (not inlined) so tests can monkeypatch main_window's
          reference and inject a pre-built dialog instead of driving a real
          blocking exec() loop. GOTCHA: comparing against
          GotoMeasureDialog.DialogCode.Accepted broke once a test
          monkeypatched the name to a plain function (no DialogCode) -
          compare against the inherited QDialog.DialogCode.Accepted instead.
          GOTCHA: QAction/QMenu objects built as local variables in
          setup_menu() get silently garbage-collected (PySide's Python
          wrapper dies even though the underlying C++ QMenu is still alive
          and parented, producing "libshiboken: Internal C++ object already
          deleted") - every QAction must be kept as a self.* attribute.
        > FOLLOW-UP 2026-08-06 (live-tested refinements): Ctrl+G opens Goto
          Measure pre-filled with the current bar. "Move to First/Last
          Measure" renamed "...Note" to match actual behaviour
          (navigate_timeline_home/end skip trailing rest padding, not
          literal measure 1). Introduced version.py (D-13).
        > FOLLOW-UP 2026-08-07 (live-tested refinements): GotoMeasureDialog's
          setFocus() in __init__ fired before the native window existed, so
          NVDA never caught the focus change until Tab was pressed -
          deferred to showEvent via QTimer.singleShot(0, ...). Reverted
          "Move to First/Last Note" firing from any region - live use showed
          this was exactly the confusing global-navigation behaviour the
          Discussion Point had already ruled out (D-2); both actions are now
          disabled unless Region 3 has focus, and a dedicated always-enabled
          "Move to Notes" (N) answers the "too much Tabbing back" concern
          instead. AboutDialog text split into 3 focusable QLabels so NVDA
          can Tab through it piece by piece. version.py -> 0.1.3.


###
PHASE D - SCREEN READER ANNOUNCEMENTS                     (4 tasks)
###
SKIPPED FOR NOW 2026-08-07: default NVDA behaviour (reading whatever gains
focus) already covers the app adequately - forcing announcements isn't worth
building speculatively. Revisit D1-D4 if a later feature (e.g. phase E
playback) turns out to need a forced/interrupting announcement that focus
changes alone can't provide. Phase E's design below deliberately avoids
depending on D1-D4 - tempo feedback lands as a 4th status bar field (E2)
rather than a forced announcement, and E4-E7 already update the regions via
the existing _update_timeline_views path rather than inventing a new
announcement channel.

[ ] D1  Spike: how to force an NVDA / VoiceOver announcement from PySide6.
        Decide the mechanism and record it as D-3 below - Ref 1 AC4
[ ] D2  Announcement service module; announce region name on region change
        - Ref 1 AC4
[ ] D3  Announce measure number on measure jump and bar goto - Refs 3, 5, 6
[ ] D4  Interrupt in-progress speech when navigation keys are held
        - NFR-05 AC-05.2


###
PHASE E - PLAYBACK ENGINE                                 (10 tasks)
###
PLANNED 2026-08-07 (E1-E7 detailed below; E8-E10 deliberately left as
one-liners - metronome is being left for another session). Three scope
decisions made while planning, all confirmed with the user rather than
assumed:
  - F/S/D and the Space/Ctrl+Space/Shift+Space playback keys are GLOBAL
    (WindowShortcut context, like the existing Ctrl+M/F6), not Note-region-
    scoped like arrow-key timeline navigation. Rationale: D-2 scoped timeline
    navigation to the Note region because those keys mean something else in
    other regions (e.g. Up/Down moving table rows); tempo/transport controls
    have no such collision and are playback-wide settings, not per-row
    navigation.
  - Tempo feedback without Phase D: a 4th status bar field ("Playback tempo:
    X bpm"), not a forced announcement. Reachable via F6/Tab like the
    existing three (C6) - something to read on demand, consistent with the
    Phase D skip above.
  - Multi-tempo scope: MusicData.tempo_bpm stays a single score-wide value
    (today's reality - no sample score has more than one tempo mark). The
    30-300 hard bounds (Ref 12 AC2) are enforced by clamping the OFFSET to
    [30 - tempo_bpm, 300 - tempo_bpm] once at load, not by tracking tempo
    per-position through TimelineBuilder the way time signature/key are
    (A6/C6/D-11). Revisit as its own task if/when a real multi-tempo score
    shows up, same pattern A6/C6 themselves followed.
    > REVISITED 2026-08-07: the user generated "Slow then quick" (files/
      Slow then quick/score.xml, 5 measures, tempo 100 -> 80/140 -> 60) and
      reported playback ignored every marking after the first. Per-position
      tempo tracking built after all - see E1's second FIX note below.

[x] E1  Separate playback tempo from score tempo; enforce 30-300 bpm - Ref 12 AC1/AC2
        > DONE 2026-08-07. playback_tempo_offset clamped once at write time
          to [MIN-tempo_bpm, MAX-tempo_bpm] (single fixed tempo_bpm, so one
          clamp suffices - see the multi-tempo scope decision above).
          tempo_bpm itself (Region 1's display) is never written to.
        > FIX 2026-08-07 (live-tested, Chessel Duet eighth=96): offset and
          the 30-300 clamp were computed in tempo_bpm's own quarter-note-BPM
          units, not the score's own display units (A9) - raising by "10"
          via F silently meant 10 quarter-BPM (20 in the units the user
          actually reads/types). playback_tempo_offset now lives in display
          units; effective_tempo_bpm() (quarter-note BPM, used internally by
          playback) converts back via the beat-unit ratio A9 already
          computed but never exposed numerically. GOTCHA WORTH REMEMBERING:
          any new tempo-facing field must be explicit about which unit
          system it's in (internal quarter-BPM vs the score's own display
          unit) - this bug recurred twice more (E2, E3) from the same
          conflation before it was fully stamped out.
        > FIX 2026-08-07 (second round, real mid-score tempo changes):
          TimelineBuilder now walks every <sound tempo>+<metronome> marking
          in the file, not just the first - MusicData.tempo_changes, queried
          via _tempo_change_at(index) so tempo-reading methods respect
          whichever marking is in effect at the cursor, not just the
          score's opening tempo. <sound tempo="X"> is always quarter-notes-
          per-minute regardless of the marking's own beat unit - only the
          beat-unit label needs translating.

[x] E2  Tempo up/down in 10 bpm steps and a reset control - Ref 12 AC3/AC4
        > DONE 2026-08-07. F/S/D (tempo faster/slower/reset) are GLOBAL
          shortcuts (WindowShortcut), not Note-region-scoped, since tempo is
          a playback-wide setting with no key collision like arrow-key
          navigation has (see the Phase E scope note above). Status bar's
          4th field shows "Playback tempo: X bpm (score default)" when the
          offset is zero, so it's obvious no offset is applied.
        > FIX 2026-08-07: same units bug as E1 - status field showed raw
          quarter-note BPM instead of the score's display units.
        > FOLLOW-UP 2026-08-07: 5th status bar field, "Playback:
          Stopped/Playing/Paused" (Ref 10), added after live testing showed
          no way to check playback state without listening. Phrase audition
          (E6) and pause/resume must NOT trigger a full status bar refresh
          (would disturb the position fields the user specifically praised
          staying put) - new set_field(index, text) updates just one field.

[x] E3  Tempo dialog accepting decimal values - Ref 12 AC5
        > DONE 2026-08-07. This is a TEMPO OFFSET dialog, not an absolute-
          tempo one, per the user's reasoning: the score's own tempo can
          vary by position (multi-tempo, E1's second FIX), and an absolute-
          value dialog would fight that. An out-of-range value is CLAMPED on
          accept, not rejected (matches AC2's "boundaries are enforced").
        > FIX 2026-08-07: same tempo-units bug as E1/E2 (dialog prepopulated
          "82" for an offset the user had set to "130") - no logic change
          needed once E1's fix landed, only the label, which now names the
          actual unit so entered/displayed numbers can't be ambiguous again.
        > FOLLOW-UP: opens on Ctrl+T too, same scope as Ctrl+G.

[x] E4  Sequencer: schedule events over time from a start index, with
        stop/pause/resume. Foundation for E5-E9.
        > DONE 2026-08-07. New EventSlice.quarters_from_start (a running
          total independent of the ts-relative display units) makes real
          elapsed-time scheduling possible. Single QTimer, rescheduled one
          step at a time (not N pre-scheduled timers) so a mid-playback F/S
          tempo change just cancels and reschedules the next step rather
          than unwinding a queue built with stale timing - tempo is re-read
          live on every step. Sequencer takes an injectable `timer`
          constructor argument so tests drive scheduling deterministically
          via a FakeTimer instead of a real wall-clock wait.
          get_playback_events_for_indices()/_visible_notes() gained an
          optional `index` parameter so the Sequencer can read an arbitrary
          slice without disturbing active_event_index/Region 3's selection
          (the update_cursor flag - phrase audition, E6, needs this).
        > FIX 2026-08-07 (live-tested, "pause is not working"): pause() left
          the sounding note ringing out on its own natural duration instead
          of silencing it - for a short note that's under a second either
          way, so there was no audible confirmation pause had done anything.
          pause() now also calls synth.stop_all_notes() immediately.

[x] E5  Play / pause / stop keystrokes; pause refreshes the regions to the
        new position; stop reverts to the original start - Ref 10
        > DONE 2026-08-07. Space/Ctrl+Space are global (playback transport,
          not timeline navigation, so D-2's Note-region scoping doesn't
          apply - same as Ctrl+M). Each Sequencer step sets
          active_event_index and refreshes the regions, which satisfies
          AC4's "pausing refreshes the regions" for free.
        > FIX 2026-08-07: Space on the last active note now plays the
          boundary cue instead of starting playback with nothing ahead to
          play.
        > FIX 2026-08-07 (second round): Space while paused was treated the
          same as Space while playing (stop + revert to original start), so
          resuming after Ctrl+Space needed two Space presses. toggle_play_stop()
          now checks is_paused first and resumes instead. Ctrl+Space
          narrowed to pause-only (it used to also resume on a second press,
          colliding with Space's new job).
        > FIX 2026-08-11 (live-tested, user decision): reaching the end of
          the piece naturally (AC2) left the cursor on the last note played
          instead of reverting to the original start (AC5) - Sequencer.stop()
          already did the revert for an explicit interrupt, but natural
          completion in _advance() just flipped is_playing off in place,
          on the reading that AC5's "stopping" meant interrupting only.
          Asked the user directly: AC5 now also applies when the end is
          reached on its own. _advance() reverts _current_index to
          _original_start_index there too; MainWindow._on_sequencer_finished
          syncs active_event_index/the regions to match for a cursor-
          tracking run (Space), the same way _stop_sequencer already did for
          an explicit stop.

[x] E6  Two-bar phrase audition on Enter; re-trigger while playing stops it - Ref 11
        > DONE 2026-08-07. Fills in the "buffer empty" Enter branch C4 left
          inert. update_cursor=False (E4) means active_event_index is
          untouched once the phrase finishes - this is a listen-to-context
          action, not navigation. Enter stops ANY active Sequencer run, not
          just another phrase (symmetric with Space stopping a phrase) - the
          existing update_cursor flag already distinguishes a
          cursor-tracking run from a phrase one, so no separate "is a phrase
          auditioning" flag was needed.

[x] E7  Chord audition on Shift+Space, holding until released or re-pressed - Ref 13
        > DONE 2026-08-07. Per the user's clarification, no keystroke-held
          tracking needed - each note's own duration already satisfies "held
          for its marked length", and "or pressed again" is satisfied by a
          plain re-trigger (SynthEngine.play_chord already stops previously-
          active notes before starting new ones, A8). Needed no Sequencer
          involvement at all - one new global QShortcut.

BUG FIX 2026-08-07 (reported hangover from E4/E5, live-tested): Sequencer
reaching the last note flipped is_playing False and emitted finished()
immediately - before the note had actually rung out - so a Space pressed
right after took the "start a new run" branch instead of "stop", and since
the cursor was already on the last note that played the boundary cue instead
("no man's land"). Fixed by scheduling a genuine ring-out wait
(_pending_next_index left None as the signal this wait is the finish, not a
further step) before flipping is_playing False - applies to both full
playback and phrase audition (E6), same code path.

[x] E8  Metronome: click every beat, accented click on beat 1, tick when
        stepping onto a beat position - Ref 14 AC1-AC3
        > DONE 2026-08-07. Reuses the existing in-process FluidSynth engine
          (confirmed with the user) rather than a second audio backend, to
          stay inside the pipeline already characterised for Ref 9 latency.
          play_click() is fully independent of the melodic note pipeline -
          doesn't call stop_all_notes() (so it doesn't cut off a note
          sounding at the same beat) and isn't touched by a later
          play_chord() (so it rings its own fixed duration regardless of the
          next note's length).
        > FIX 2026-08-07 (live-tested, "was awful"): swapped the synthesized
          sawtooth lead for GM percussion Claves. GOTCHA: GM percussion only
          maps correctly on MIDI channel 10 (0-indexed 9), so
          METRONOME_CHANNEL moved there from an arbitrary 14 - not a
          collision with a score's own percussion part, since multiple
          percussion notes legitimately sound at once on channel 10 same as
          a real drum kit. Percussion notes have no tunable pitch, so beat
          1's accent is now velocity (127 vs 100), not a different note
          number. See D-14 - still not a satisfying sound (E11 open).

[x] E9  With metronome on, beat positions become navigable events even where
        no note sounds - Ref 14 AC4, depends on A4
        > DONE 2026-08-07. IMPORTANT CORRECTION mid-task: the first cut
          merged synthetic beat markers directly into timeline_slices
          unconditionally at parse time ("cheap, harmless when metronome is
          off") - broke 9 existing tests, because CLAUDE.md's documented
          Timeline model ("one entry per distinct (measure, offset) WITH AT
          LEAST ONE SOUNDING NOTE") is relied on by direct-indexing tests
          throughout the suite. Fixed properly: beat_markers is kept as a
          SEPARATE list; set_metronome_enabled() only merges it into
          timeline_slices when turning ON and restores the untouched real
          list when turning OFF, relocating active_event_index to the
          nearest equivalent real position across the rebuild.
          _slice_has_visible_sounding_note/_sounding_bounds deliberately
          UNCHANGED - the outer Left/Right/Home/End boundary of the piece
          stays anchored to real sounding notes only, so metronome mode
          can't resurrect trailing rest-only padding as navigable (though
          the Sequencer's own playback walk WILL click through trailing
          padding during a full play-through - consistent with AC1 having
          no such exclusion). GOTCHA WORTH REMEMBERING: never merge
          synthetic/derived slices into timeline_slices unconditionally -
          plenty of code assumes that list is exactly the real, sounding
          timeline.

[x] E10 Latency harness; verify audition stays within 25 ms - Ref 9 AC3, NFR-04
        > DONE 2026-08-07. New latency_harness.py at the repo root, not
          tests/ - it needs a real SynthEngine, which tests/conftest.py's
          autouse fixture deliberately blocks (D-7); run by hand via
          `.venv\Scripts\python.exe latency_harness.py`, no pass/fail
          assertions, just a printed report (no hardware-backed CI to assert
          against). Measures per-note dispatch latency and Sequencer
          scheduling jitter. Verified live: play_notes mean 0.023ms/max
          0.341ms, play_click mean 0.012ms/max 0.018ms (both far under the
          25ms budget); Sequencer jitter -3.4ms to +0.6ms at 240bpm.

BUG FIX 2026-08-12 (reported by the user, live-tested against
"pachelbels-canon-in-d-string-quartet.mxl" - examples/, real playback,
not a fixture): two separate note-off bugs, both traced to A8's
play_chord(events, duration_ms) treating duration/silencing as a single
value shared across an entire call, rather than per group:
  1. EventSlice.quarter_length (parsers/timeline_builder.py) is min() across
     EVERY note bucketed into a slice - i.e. across every part sounding at
     that instant, not just one chord. get_duration_ms_for_index/
     get_current_duration_ms converted that single clamped value to note-off
     timing for the WHOLE chord, so the cello's minims in the Canon were
     truncated to whatever the fastest-moving upper part's note value was at
     that beat. FIX: MusicData.get_playback_events_for_indices/
     get_playback_events_at_index now return (channel, program, midi_notes,
     duration_ms) 4-tuples, duration_ms computed per PART from that part's
     own notes' quarter_length (max, not min/average, so a same-voice chord
     with slightly inconsistent data still rings for its longest member).
     SynthEngine.play_chord/NullSynth.play_chord schedule one independent
     QTimer per group instead of one shared timer. EventSlice.quarter_length
     itself is untouched (still needed for the Sequencer's end-of-run
     ring-out wait when there are no events, and one characterisation test);
     nothing in production playback reads it as a note's own duration
     anymore.
  2. Once (1) was fixed, a second bug surfaced: turning on Violin I (enters
     on beats 2/4) caused Violin II/Viola/Cello's beat-1 minims to cut short
     to one beat the moment Violin I's note started - "like Violin I was
     sending a MIDI off to the other parts" (the user's own diagnosis,
     exactly correct). ROOT CAUSE: SynthEngine.play_chord unconditionally
     called stop_all_notes() before every call, correct for discrete
     audition (Ref 8 AC2 - Region 3 navigation/chord audition should present
     a clean slate on every move) but wrong for the Sequencer's real
     step-to-step playback advance, which went through the exact same
     method. FIX: play_chord(events, duration_ms=250, retrigger=True) -
     Sequencer._sound_current_step now passes retrigger=False for its
     natural advance, so a new part's attack no longer silences other parts'
     still-ringing notes; Sequencer.play_from() now calls
     synth.stop_all_notes() itself before sounding the new position, so an
     explicit reposition (not a natural advance) still clears the deck.
     New fixture tests/fixtures/staggered_two_part_entry.musicxml (one
     part sustaining across another part's later, unrelated entry)
     regression-tests this directly.

BUG FIX 2026-08-12 (reported by the user, live-tested with a real guitar and
independently against Reaper/reaSynth - every note played about a semitone
sharp, e.g. a written E sounded like F): initially suspected the reported
soundfont (FluidR3_GM's nylon guitar patch), MIDI pitch computation, and a
sample-rate mismatch against the user's actual audio device - all three
tested and ruled out directly (offline fluidsynth.Synth.get_samples()
rendering + autocorrelation pitch measurement, no real audio device needed;
music21 cross-check on the MIDI number; user confirmed their device is
already 48kHz). ROOT CAUSE, found by reproducing the app's own
_init_engine() sequence in that same offline harness: SynthEngine._init_engine
built the synth via `fluidsynth.Synth(gain=0.7)` (no samplerate= argument, so
pyfluidsynth's own 44100 Hz default applied) and only set the intended 48000
Hz afterward via `self._fs.setting("synth.sample-rate", 48000.0)`. But
pyfluidsynth.Synth.__init__ calls new_fluid_synth(self.settings) - creating
the actual DSP engine - using whatever synth.sample-rate is in the settings
object AT THAT MOMENT, before returning; a .setting() call afterward only
updates the stored value, it does not reinitialize an already-created
engine. So FluidSynth was silently rendering audio at 44100 Hz the whole
time while WASAPI opened the output stream at 48000 Hz (to match the real
48kHz device) - playing 44100 Hz worth of samples through a 48000 Hz stream
speeds everything up by 48000/44100 (~1.088x), which shifts pitch up ~147
cents, landing closer to a semitone above (F) than a whole tone (F#) -
exactly the reported symptom. GOTCHA WORTH REMEMBERING: pyfluidsynth's
synth.sample-rate (and likely other settings read at engine-creation time)
MUST be passed to the Synth() constructor itself, not set afterward via
.setting() - the constructor's own samplerate=44100 default silently wins
if omitted. Fix: `fluidsynth.Synth(gain=0.7, samplerate=48000.0)`. Verified
by reproducing both the broken and fixed init sequences offline and
measuring the actual rendered frequency each way; user confirmed live on
real playback afterward. Considered - and explicitly declined by the user in
favour of the existing hardcoded 48kHz - having SynthEngine query the host's
actual default output device format and render at that native rate instead
of relying on WASAPI shared-mode's own resampling to adapt a fixed 48kHz
stream to whatever the real device runs at; shared-mode resampling is
standard, expected OS behaviour (not something this bug depended on - the
bug was an internal 44100-vs-48000 inconsistency within our own code, not a
mismatch against the device), so no change was needed for other users on a
44.1kHz device.

[x] E11 Metronome click sound quality - functional debt, not a missing
        feature (Ref 14 AC1-AC3 are all functionally met by E8) - see D-14.
        > OPENED 2026-08-07. Two synthesis attempts, both live-tested by the
          user and found lacking: a synthesized sawtooth lead ("awful"),
          then GM percussion Claves ("better but still not really good
          enough"). Next step per the user's own original framing when E8
          was planned: probably the raw-waveform-or-WAV-asset route
          (audio/synth_engine.py's SynthEngine is FluidSynth/soundfont-only
          - no built-in tone generator - so an exact-spec click, e.g. a
          precise 1200Hz/800Hz sawtooth, needs either a small procedurally-
          generated PCM path alongside FluidSynth's WASAPI stream, or two
          short user-supplied WAV clips) - deliberately not attempted yet;
          revisit when picked up, including checking whether a second
          concurrent audio stream competes with FluidSynth's own
          low-latency WASAPI session (Ref 9).
        > DONE 2026-08-12. Went with real recorded WAV clips ("accent"/
          "offbeat") rather than synthesis, resolving both this and Ref
          28's own open "how does the position announcer play WAV clips"
          question the same way: tools/wav_to_sf2.py (new standalone
          script, see tools/README.md) builds a small custom soundfont
          from a plain-text config (tools/config.ini, the user's own,
          references tools/words1/ and tools/click1/ WAV recordings) -
          checked into git as soundfonts/recall_score_sounds.sf2(.json)
          despite soundfonts/ otherwise being gitignored (.gitignore has
          an explicit `soundfonts/*` + negation exception, since a bare
          `soundfonts/` directory-rule silently defeats a later per-file
          negation - a real gotcha hit while making this change, worth
          remembering). SynthEngine._load_click_soundfont loads it as a
          SECOND soundfont (its own sfid) alongside FluidR3_GM, staying
          entirely inside the existing FluidSynth/WASAPI session per the
          precedent above - no second audio stream. audio/metronome.py's
          click_event_for_beat now returns (channel, bank, program,
          pitch, velocity) - accent vs regular beat is which SAMPLE plays
          (two different recorded sounds, program_select onto the
          dedicated click soundfont) rather than a velocity difference on
          one fixed Claves voice. SynthEngine.play_click no longer takes
          duration_ms as a parameter - a one-shot sample only plays to
          its natural end if held that long, and unlike a real note there
          is no score position to read a duration from, so
          _load_click_soundfont reads each sample's real length from the
          sidecar recall_score_sounds.sf2.json (built by the tool from
          each WAV's own frame count) into self._click_durations_ms,
          keyed by (bank, program, pitch) - NOT a flat per-note dict,
          since the talking metronome's words and the click's sounds can
          legitimately share note NUMBERS across different presets.
          VERIFIED, not just "the bytes parse": rendered the generated
          soundfont through real FluidSynth (fluidsynth.Synth.
          get_samples(), no audio device needed) and measured that
          channel 9 (METRONOME_CHANNEL, chosen historically for GM
          percussion's channel-10 behaviour, D-5) does NOT still force
          bank 128 percussion lookups now that a custom soundfont is
          program_select'd there instead of relying on GM - correlation
          0.999 against the identical render on an ordinary channel.
          Packaging (packaging/RecallScore.spec) updated to bundle the
          new soundfont+sidecar the same way as FluidR3_GM.sf2.

[x] E12 Position announcer ("talking metronome") - Ref 28, new requirement
        added 2026-08-12 (see the PDD's own Ref 28 row and Discussion
        Points entry for the full AC list and worked examples).
        > DONE 2026-08-12. New audio/position_announcer.py (mirrors
          audio/metronome.py exactly): spoken_word_for_beat_position()
          splits a ts-relative beat_position into whole part (1-7 -> word,
          AC3) and rounded fractional remainder (25/33/50/66/75% -> e/and/
          and/a/a, AC4) - verified against all three of the user's own
          worked examples (4 semiquavers from bar start -> one/e/and/a; 3
          quavers in 6/8 -> one/two/three; a triplet from beat 2 -> two/
          and/a). announcement_event_for_beat() wraps that into a (channel,
          bank, program, pitch, velocity) tuple, same shape as
          click_event_for_beat(). Hookup mirrors the click exactly:
          Sequencer._sound_current_step and MainWindow.
          _play_selected_region_3_notes both call it independently of the
          metronome (AC1), MusicData.position_announcer_enabled gates it
          with no timeline_slices rebuild at all (unlike
          set_metronome_enabled) - AC5 means it can never create its own
          events, so nothing needs splicing in/out. Ctrl+P / Options menu
          checkable action / 7th status bar field, all mirroring Ctrl+M's
          own three.
          Real design problem, not just wiring: tools/config.ini's
          talking_metronome_default preset (notes 60-69) and click_default
          (notes 60-61) overlap at 60/61. AC2 requires both to sound
          together (e.g. beat 1's accent click + "one"), and FluidSynth's
          noteoff(channel, key) releases every voice matching that pair
          regardless of which preset/program started it - so a shared
          channel would let one sample's note-off silence the other
          whenever they land on the same beat. Fixed by giving the
          announcer its own reserved MIDI channel (audio/
          position_announcer.py's POSITION_ANNOUNCER_CHANNEL = 8) alongside
          the click's existing channel 9, both skipped by MusicData.
          get_channel_for_part when allocating real instrument parts
          (RESERVED_CHANNELS, generalised from the old single-channel
          PERCUSSION_CHANNEL skip) - considered and rejected the
          alternative of asking the user to renumber the word notes to a
          disjoint range and rebuild their soundfont instead, since a
          reserved channel is a systemic guarantee that holds for any
          future click/word preset pair, not a numbering convention that
          has to be remembered every time. SynthEngine.play_word() is a
          near-duplicate of play_click() with its own active-note/QTimer
          slot for exactly that reason, sharing the same _click_sfid (one
          soundfont, loaded once, covers both presets already) and the
          same sidecar-duration lookup. Caught mid-implementation: a
          class-body list comprehension can't see another class attribute
          except in its own outermost iterable (RESERVED_CHANNELS raised
          NameError when referenced inside the comprehension's condition)
          - fixed by computing the usable-channel list inside
          get_channel_for_part() at call time instead of caching it as a
          class attribute.
        > FIX 2026-08-12. Two user follow-ups after live-testing (piano
          silence in a separate test file turned out unrelated to this
          task - traced via a temporary regression test copying the file
          through MainWindow's real load path with a recording synth
          stand-in, confirmed channel/program/pitch/duration dispatch was
          all correct, left as "not something I did wrong", nothing
          changed here):
          (1) "why does the soundfont need a json file? can't we make one
          that doesn't need it?" - turned out the sidecar (bank, program,
          pitch) -> duration_ms lookup this task built was never actually
          necessary. Verified against real FluidSynth (get_samples(), no
          audio device needed): rendering a one-shot SampleModes=0 zone
          with NO note-off sent at all reproduced its full natural length
          with no hang, because FluidSynth deactivates such a voice on its
          own once the sample data is exhausted regardless of note-on/off
          state - only an EARLY note-off (before the sample naturally
          finishes) triggers the soundfont's default near-instant release
          envelope and audibly truncates it, which is the one thing that
          actually needed avoiding. Removed self._click_durations_ms, the
          per-click/word QTimer scheduling, and DEFAULT_CLICK_DURATION_MS
          from SynthEngine entirely - play_click/play_word just noteon and
          let the sample finish on its own; _stop_click/_stop_announcement
          (still called from stop_all_notes and from a retriggering
          play_click/play_word) remain the deliberate-interrupt path, just
          no longer timer-scheduled. tools/wav_to_sf2.py no longer writes
          a .sf2.json sidecar at all (build_metadata() and SampleZone.
          duration_ms removed as dead code); soundfonts/recall_score_
          sounds.sf2.json deleted (was never git-tracked) and its
          .gitignore/RecallScore.spec entries removed.
          (2) "can the talking metronome be panned full left, and the
          click full right, to help separate them from the music?" - made
          easy by (1)'s own channel reservation: SynthEngine.
          _load_click_soundfont now sends MIDI CC10 (pan) once per
          reserved channel at load time - POSITION_ANNOUNCER_CHANNEL full
          left (0), METRONOME_CHANNEL full right (127) - rather than
          per-call, since a channel's pan persists until changed and both
          channels are permanently dedicated (never reused for a real
          part, MusicData.RESERVED_CHANNELS). Verified against real
          FluidSynth: CC10=0 produced silence on the right channel,
          CC10=127 silence on the left.


###
PHASE F - NOTE DISPLAY CONFIGURATION                      (4 tasks)
###
[x] F1  Formatting layer building the Region 3 label from a configurable
        attribute list, e.g. "C, string 5, fret 1" - Ref 15
        > DONE 2026-08-07. Two gaps against the literal PDD text, resolved
          with the user rather than assumed: Ref 15 AC4 names a 4th "score"
          scope beyond voice/stave/part (kept even though Ref 27 AC2 only
          mentions three); a stave/part/score-scope action fired from a
          multi-note (chord) selection unions the scope across every
          selected note, not just the one the menu was opened on.
          voice_display_attributes: Dict[(part_id, staff, voice), Set[str]],
          defaulting to {"step"} (today's plain-name behaviour) - "step" is
          itself toggleable off, rendering a blank but still navigable row.
          Region4TableWidget uses Qt's CustomContextMenu policy, assumed (at
          the time) to raise customContextMenuRequested for both
          right-click and the Menu/Application key - see the first FIX
          below, that assumption was wrong.
        > FIX 2026-08-07 (live-tested - menu unreachable by keyboard):
          ROOT CAUSE, confirmed empirically: Qt does NOT synthesize
          customContextMenuRequested from Menu/Shift+F10 for a QTableWidget
          - only a real right-click reliably raises it. GOTCHA WORTH
          REMEMBERING for any future CustomContextMenu-policy widget in this
          app. Region4TableWidget.keyPressEvent now handles
          Key_Menu/Shift+F10 explicitly, calling the same
          _show_attribute_menu path a right-click uses. Also: row/position
          resolution now uses rowAt(pos) for a real click and only falls
          back to currentRow() for the keyboard path, since a right-click on
          a different row than the keyboard-focused one should act on the
          row actually clicked.
        > FIX 2026-08-07 (live-tested - "Unknown display-attribute scope:
          False" traceback): the REAL reason no attribute was ever added, in
          every scope. ROOT CAUSE: QAction.triggered always calls its
          connected slot with one positional bool ("checked"). The per-scope
          menu callback was `lambda scope=scope: ...` - Qt/PySide calls a
          connected Python callable with one positional arg whenever it
          accepts one, so that bool landed in scope's own default-arg slot
          instead of "voice"/"stave"/etc. Invisible to this session's own
          tests, which all called the callback with zero arguments - never
          exercising the argument a real QAction actually passes. Fix:
          lambda now takes `checked=False, scope=scope` - checked soaks up
          the bool, scope keeps its loop default. GOTCHA WORTH REMEMBERING:
          any QAction.triggered-connected lambda in this codebase needs a
          leading throwaway positional/keyword arg, or it silently
          misassigns whatever real argument comes first - and a test that
          calls such a callback with zero arguments cannot catch this class
          of bug.
        > FIX 2026-08-07 (two more findings):
          1. FIXED - no NVDA announcement on Application-key press, only
             after arrowing once. menu.exec(global_pos) was called with no
             `at` argument, so nothing was pre-highlighted. GOTCHA: QMenu.exec
             is NOT reliably monkeypatchable in this PySide6 build - a test
             that tried `monkeypatch.setattr(QMenu, "exec", fake_exec)`
             silently fell through to the real blocking call and hung the
             suite (confirmed twice). Worked around by splitting menu
             construction from the exec() call so tests can assert against
             the built-but-not-exec'd menu instead.
          2. A "attribute not actually added" report could not be
             reproduced this round - turned out to be a stale running
             process, confirmed in the next FIX entry.
        > FIX 2026-08-07: confirmed the "not added" report was the user
          testing a stale process (old code still running), not a real bug.
          Also confirmed Region 3's attribute rendering order is ALWAYS the
          fixed DISPLAY_ATTRIBUTE_ORDER regardless of which scope an
          attribute was added through (real ordering control is F2, still
          open). Two more menu-interaction bugs:
          1. FIXED, high confidence - ROOT CAUSE: selecting a menu action
             rebuilds Region 4's rows WHILE the menu's own exec() is still
             running (QAction.triggered fires before exec() returns), which
             reset the table's current cell - so Enter (which triggers a
             rebuild) lost the row, while Escape (no rebuild) correctly kept
             it. Fixed with _restore_region_4_focus_after_menu(row, column),
             called after exec() returns.
          2. BEST-EFFORT, reverted next round (see below) - tried
             pre-highlighting the first menu item via exec(pos, at=) plus a
             synthetic Key_Down QKeyEvent, to get an immediate NVDA
             announcement without a manual arrow press.
        > FIX 2026-08-07 (confirms the focus-restoration fix works):
          1. FIXED - opening the menu from Region 4's VALUE column returned
             focus to the KEY column - the column was hard-coded to 0 in two
             places; both now thread the actual currentColumn() through.
          2. REVERTED - the prior round's synthetic-announcement attempt
             (exec(pos, at=first_action) + synthetic Key_Down via
             QApplication.sendEvent) did NOT work: live-tested, the
             Application key announced "Python window" and NVDA+Up reported
             "blank" instead of the item text - something was genuinely
             highlighted (the user's next Down landed on the SECOND item)
             but carried no real accessible name. GOTCHA WORTH REMEMBERING:
             Qt's accessibility bridge only hands UIA/MSAA focus down to a
             menu-item sub-object on a GENUINE keyboard-navigation event,
             not a programmatic setActiveAction/exec(at=)/sendEvent one -
             don't re-attempt this without a way to verify against real
             NVDA. Reverted to a plain menu.exec(global_pos) (one manual
             arrow press needed - "more normal" anyway, per the user).

[x] F2  Options dialog to control note-display attribute ORDERING - Ref 15
        AC4. Scope control (global/part/staff/voice - now voice/stave/part/
        score) shipped with F1 above via Region 4's context menu instead of
        a separate Options dialog; only the ordering half remained open.
        > DONE 2026-08-10, not yet live-tested. Options > Reorder
          Attributes... opens a dialog scoped to whichever part/staff/voice
          is selected in Region 2, listing only the attributes present
          anywhere in the score for that scope (MusicData.
          attribute_keys_for_voices), with Move Up/Move Down buttons
          (Alt+U/Alt+D mnemonics) reordering them. The order itself is a
          single global list (MusicData.attribute_order), not per-voice -
          Region 3 and Region 4 both render from it now (previously Region
          4's row order was a separate hardcoded sequence that only
          happened to match DISPLAY_ATTRIBUTE_ORDER). Session-scoped like
          uk_terms: MainWindow keeps its own copy and reapplies it after
          every file load rather than letting a fresh MusicData reset it.
          Deliberately does NOT duplicate F1's add/remove context menu - the
          dialog only reorders. Whether focus should land on the list row
          vs. stay on the clicked Move button after each move (currently:
          moves to the list) is a live-testing question, same as several
          other focus/NVDA-timing decisions in this phase.
[x] F3  Capture further attributes from the source file: dynamics, staccato,
        fingering - Ref 16 AC3
        > DONE 2026-08-10, live-tested against a real MuseScore export
          ("Fingers and articulations"). NoteData gained dynamic/
          articulation/fingering/pluck (Optional[str]) - TimelineBuilder
          parses notations/articulations + notations/ornaments (merged into
          one comma-joined "articulation" field), notations/technical/
          fingering, and notations/technical/pluck (guitar RH p/i/m/a).
          Dynamics are trickier: MuseScore emits a <direction> sibling of
          <note>, not a child of it, so a per-measure pending_dynamics dict
          keyed by (staff, offset_divs) matches the mark to whichever
          note(s) land at that same offset - chord notes share the base
          note's offset so they inherit it for free, no special-casing
          needed. notations/dynamics directly on a note (rarer) overrides
          the direction-based match when present. New models/vocabulary.py
          dynamic_name()/articulation_name() give spoken forms (f->forte,
          strong-accent->marcato, trill-mark->trill...), falling back to raw
          text for anything unmapped.
          No Region 3/4 UI work was needed at all - F1's existing
          voice_display_attributes/DISPLAY_ATTRIBUTE_ORDER/
          _note_attribute_pairs machinery (Ref 15 AC4) already shows a key
          only when it's both toggled on for that voice AND present on the
          note, so the four new keys just slot into DISPLAY_ATTRIBUTE_ORDER
          and _note_attribute_pairs and the context menu/scope toggles work
          unmodified.
          Piano LH/RH hand is deliberately NOT inferred from staff/clef -
          MusicXML's <fingering> carries no hand flag, so both hands render
          under one generic "fingering" key and the existing "stave"
          attribute supplies hand context when toggled on alongside it
          (user-confirmed choice over guessing from clef).
        > FIX (live-tested against the real file, two findings):
          1. notations/technical can carry MORE THAN ONE <fingering>/
             <pluck> on a single note (MuseScore's rasgueado marking: one
             note, three <pluck> children p/i/m/a together). The original
             code used ElementTree .find(), which only returns the first
             match - silently dropped the rest. Switched to .findall() +
             ", "-join for both fields. Regression fixture
             tests/fixtures/multi_value_technical.musicxml added.
          2. NVDA didn't announce a slice's note automatically when Left/
             Right navigation landed on exactly ONE note - only Up/Down
             worked. ROOT CAUSE: _update_timeline_views' rebuild calls
             region_3.clear() (resets the view's "current" item to none)
             then selectAll() (marks rows selected but deliberately leaves
             "current" untouched) - so after a rebuild there was never a
             definite current/focused item for the accessibility bridge to
             point at. With several notes selected, NVDA still gets a
             selection-changed announcement; with exactly one, nothing told
             it which item that was. Fixed by adding
             region_3.setCurrentRow(0) after blockSignals(False) (so the
             selection model's real currentChanged signal fires) - the
             1-arg form uses QItemSelectionModel::NoUpdate so it doesn't
             disturb the selectAll() selection. This is a general Region 3
             fix, not F3-specific (any rebuild landing on a single-note
             slice had the same gap). Regression test
             test_navigating_onto_a_single_note_slice_sets_a_current_row
             added, verified to fail without the fix via git stash.
[x] F4  UK / US music vocabulary toggle: bar vs measure, crotchet vs quarter
        note, quaver vs eighth note - Discussion Points, see D-6
        > DONE 2026-08-10. Purely presentational per the user - internal
          identifiers (attribute_key "measure", method names, MusicXML type
          strings) never change, only rendered text. New models/vocabulary.py
          (Qt-free) holds the word-choice/translation helpers; MusicData
          gained uk_terms: bool (default False - required for backward
          compatibility with every existing test's US-dialect assertions).
        > Default dialect: UK, unless QLocale.system().territory() resolves
          confidently to United States, in which case US - any other/
          indeterminate locale stays UK (never a guess). Lives in
          main_window.py, not models/, since it needs Qt; injectable via a
          new MainWindow(uk_terms=...) constructor param (same D-7 DI
          pattern as synth) so tests get a deterministic result regardless
          of the machine running them - GOTCHA hit live: this dev machine's
          own OS locale is en_GB, so the existing test suite (previously
          assuming US wording unconditionally) broke wholesale until the
          window fixture pinned uk_terms=False explicitly.
        > uk_terms is a SESSION preference, not a per-score one (unlike
          metronome_enabled/voice_display_attributes) - MusicData is wholly
          replaced on every file load, so _on_score_loaded explicitly
          reapplies MainWindow's own self._uk_terms onto each freshly-loaded
          MusicData, and set_uk_terms() (unlike toggle_metronome) still
          works with no score loaded. Region 1's "Tempo" credit is baked
          once at parse time in US units (MusicXMLReader) - get_region_1_data
          now overrides it live from tempo_bpm/tempo_beat_unit_quarter_length
          directly rather than returning self.credits verbatim, so toggling
          is reflected without reloading the file.
        > FIX 2026-08-10 (live-tested, three findings):
          1. Menu shape wrong - a single checkable "UK Terms" action doesn't
             show "at least one option is always active" as clearly as the
             user wanted. Replaced with Options > Terminology Language, a
             submenu of two mutually exclusive checkable items (UK/US) via
             QActionGroup(exclusive=True) - clicking one always leaves
             exactly one checked, satisfying "must have at least one ticked"
             structurally rather than by convention. toggle_uk_terms()
             replaced by set_uk_terms(bool), called from two thin
             _select_uk_terms/_select_us_terms wrappers (the usual
             QAction.triggered(bool) gotcha - see F1's postmortem - handled
             by giving each its own named callback instead of a scope=scope
             lambda).
          2. Region 1/4's current cell was jumping to the top-left cell on
             every terminology-language change. ROOT CAUSE: _populate_table
             (the shared Region 1/4 rebuild helper) calls clearContents()/
             setRowCount(), which resets QTableWidget's current cell to
             (0, 0) - true for ANY refresh through this method, not just the
             F4 toggle. Fixed at the source: _populate_table now captures
             current row/column before rebuilding and restores them after
             (clamped to the rebuilt table's bounds), so this is now a
             general Region 1/4 position-persistence guarantee, not
             something F4-specific. Made
             test_restore_region_4_focus_after_menu_...'s own "sanity check:
             the rebuild really did reset it" assertion obsolete (that
             method's remaining job is now specifically returning WIDGET
             FOCUS after a QMenu.exec(), not the cell position - both used
             to be conflated in one bug).
          3. D-15 opened and resolved live - see below: stave/staff excluded
             from the toggle entirely (models/vocabulary.py's staff_word/
             clef_name removed again), Region 2/4's clef-name wording is
             unaffected by uk_terms.


###
PHASE G - PER-SCORE PERSISTENCE                           (5 tasks)   Ref 27
###
[x] G0  Fix .mxl (compressed MusicXML) loading - prerequisite, not itself
        part of Ref 27
        > DONE 2026-08-11. MusicXMLReader._parse_xml_root and
          TimelineBuilder.build's fallback both did a raw ET.parse(), which
          can't open a .mxl zip container - music21.converter.parse (used
          for tempo/key/time only) already handled .mxl fine, but the
          ElementTree passes that are the actual source of truth for
          parts/timeline didn't. Discovered while planning G1: every real
          .mxl in files/ (confirmed via files/Chessel Duet.mxl) contains an
          internal member literally named score.xml regardless of the outer
          filename, same as this project's own uncompressed test scores -
          so per-file config naming has to key off the outer filename, which
          only works once .mxl actually loads. New parsers/xml_source.py
          (read_musicxml_root) reads META-INF/container.xml's rootfile
          rather than guessing a member name; both call sites use it now.
[x] G1  Persist part/staff/voice toggles and metronome state per score file
        - Ref 27 AC1
        > DONE 2026-08-11. New persistence/score_config.py: ScoreConfig
          (voices_off, metronome_enabled, voice_display_attributes,
          attribute_order) + config_dir/path_for/load_for/save/delete_for.
          Keyed purely by the music file's own basename+extension (e.g.
          "Chessel Duet.mxl.rsc") - no path/location component, so moving
          the file around still finds the same config; two different files
          sharing a name collide, accepted as a consequence of best-effort
          loading rather than engineered around (user decision).
          MusicData.export_config/apply_config do the actual save/restore;
          apply_config is best-effort by construction - voices_off (not an
          on-list) and voice_display_attributes/attribute_order are
          filtered against the freshly-loaded score's own known
          voices/attribute keys, so a saved entry that no longer matches
          (e.g. a part that's gone) is silently dropped rather than
          rejecting the whole config or showing a dialog (user decision).
          MainWindow.load_score_from_file saves the outgoing file's config
          before swapping in a new one; closeEvent saves on shutdown; both
          call _save_current_score_config. _on_score_loaded loads and
          applies the incoming file's config.
        > FIX 2026-08-12 (reported by the user, live-tested): switching a
          part off with a sub-staff/voice still individually ON, closing,
          then reopening the score correctly showed the part off - but
          switching the part back on revealed its sub-elements had ALSO
          gone off, losing their real prior state. ROOT CAUSE: the only
          thing ever persisted was voices_off, the complement of
          active_voice_filter - itself computed by
          Region2HierarchyModel.get_active_voice_tuples(), which gates a
          voice's "active" status on its PART and STAFF also being enabled.
          So a part being off collapsed every voice under it into "off" in
          the saved file - the fact that a voice was individually still on
          underneath was nowhere recorded, and restore
          (set_active_voice_tuples) could only INFER a node's own state from
          whether every leaf below it was inactive, which can't tell "the
          user toggled the part off" apart from "every voice under it
          happened to end up off" (that inference was already documented as
          a known limitation in its own docstring). FIX: ScoreConfig gained
          parts_off/staves_off alongside voices_off - all three now mean
          that node's OWN toggle state, independent of ancestors, not a
          derived "effectively active" set. Region2HierarchyModel gained
          get_off_node_keys()/apply_off_node_keys() - an ungated,
          lossless read/write of every node's own .enabled at all three
          levels - replacing set_active_voice_tuples entirely.
          MainWindow._save_current_score_config now overwrites
          export_config()'s own best-effort voices_off with the real
          per-node sets from region_2.model_manager.get_off_node_keys();
          _on_score_loaded restores them straight into Region 2 via
          apply_off_node_keys, which also let the old "snapshot the filter
          before Region 2's rebuild wipes it" workaround in _on_score_loaded
          be removed - Region 2 is now restored directly from the saved
          per-node sets rather than round-tripped through MusicData's flat
          active_voice_filter.
[x] G2  Persist note display configuration per scope - Ref 27 AC2
        > DONE 2026-08-11, same change as G1 (voice_display_attributes is
          one of ScoreConfig's fields). attribute_order (F2's ordering, not
          just which attributes are shown) is ALSO per-file - goes beyond
          Ref 27 AC2's literal wording but was explicitly requested. This
          simplified main_window.py: attribute_order was previously a
          MainWindow-tracked "session preference" (self._attribute_order,
          reapplied onto every freshly-loaded MusicData, same pattern as
          uk_terms) - that's gone now, replaced by "apply the loaded
          config's order if there is one, else keep MusicData's own
          DISPLAY_ATTRIBUTE_ORDER default."
[x] G3  Global settings persisted across sessions independent of any file
        (language) - adjacent to Ref 27, not required by its literal text
        > DONE 2026-08-11. Explicitly NOT per-file, unlike G1/G2's fields:
          UK/US terminology (F4/D-6) stays a single app-wide preference so
          switching files mid-session never changes the displayed language
          (user decision, after discussing making it per-file and rejecting
          that). New persistence/app_settings.py (AppSettings.uk_terms) -
          loaded once at MainWindow startup (falling back to
          detect_default_uk_terms() if nothing saved yet), written
          immediately on every change in set_uk_terms rather than batched
          to shutdown like G1/G2's per-file config, since it's one small
          write and losing it to a crash would be more noticeable.
[x] G4  App renamed in the title bar; title shows the loaded file; Edit
        menu (Open Local Folder / Clear Preferences) - UI work needed to
        make G0-G3 usable, not itself required by Ref 27
        > DONE 2026-08-11. Window title was still "Score View & Editor"
          (main_window.py) even though AboutDialog already said "Recall
          Score" - now "Recall Score" by default, "<filename> - Recall
          Score" once a file is loaded. New top-level Edit menu, after
          File: "Open Local Folder" (QDesktopServices to
          score_config.config_dir()) and a dynamic-label "Clear Preferences
          for <filename>" (disabled with no file loaded) that deletes that
          file's .rsc. Deliberately minimal - Ref 27's persistence is meant
          to stay mostly invisible to the user; these two entries are the
          only acknowledgement the .rsc mechanism exists at all.


###
PHASE H - TABLATURE                                       (4 tasks)
###
[ ] H1  Left and right hand finger attributes on notes - Ref 20
        > NOTE 2026-08-10: largely already delivered as a side effect of F3.
          NoteData.fingering (left/guitar-LH/piano, notations/technical/
          fingering) and NoteData.pluck (guitar RH p/i/m/a, notations/
          technical/pluck) are parsed and already toggleable in Region 3/4
          via F1's generic attribute system, same as the pre-existing
          string/fret. Ref 20 AC1 also names String Number/Fret Number/
          Voice, which were already done before F3. Before starting fresh
          work here: check whether anything is actually still missing
          (tablature-specific announcement wording? capo interaction, which
          is H2's job?) rather than assuming this is greenfield.
[ ] H2  Capo: raise playback pitch, show note names and frets relative to it - Ref 21
[ ] H3  Identify the chord sounding at the cursor and name it - Ref 22 AC1/AC3
[ ] H4  Describe fretboard shapes in words, e.g. "barred G at 3rd fret" - Ref 22 AC2


###
PHASE I - ROBUSTNESS AND ERROR HANDLING                   (2 tasks)
###
[ ] I1  Accessible error dialog for corrupt or unreadable files, replacing the
        [ERROR] prints in main_window.py:132 and music_data.py:31
        - Ref 25 AC3, NFR-06 AC-06.1
[ ] I2  Preserve unhandled custom MusicXML tags through save and export
        - NFR-06 AC-06.2


###
PHASE J - FILE I/O                                        (6 tasks)
###
[x] J1  Import MIDI, types 1 and 2 - Ref 25
        > DONE 2026-08-15. parsers/midi_source.py (hand-rolled binary MTrk
          parser, no mido - see CLAUDE.md), parsers/midi_reader.py
          (MidiReader, mirrors MusicXMLReader) and
          parsers/midi_timeline_builder.py (MidiTimelineBuilder, mirrors
          TimelineBuilder). MusicData.__post_init__ and
          workers/score_load_worker.py dispatch on .mid/.midi extension.
          Ground-truth verified: bach-bourree-tab.mid (MuseScore's own MIDI
          export of files/bach-bourree-tab/score.xml) diffed note-for-note
          against the MusicXML parse over the un-repeated prefix - see
          tests/parsers/test_midi_timeline_builder.py's
          test_matches_musicxml_ground_truth_for_the_unrepeated_prefix.
          Format 2 (asynchronous multi-track) is NOT handled - no real-world
          fixture exists for it and it's vanishingly rare; scope reduced to
          types 0/1 with the user's agreement. Percussion (channel 10)
          tracks are skipped on import, matching Ref 25 AC2's "load all
          tracks" only where a track carries real pitched note content.
          See D-19 for the "no synthetic rests for MIDI gaps" decision, and
          Phase S for follow-up fixes/features found reviewing real files.
[x] J2  Import Guitar Pro - Ref 25
        > DONE 2026-08-16. Hand-rolled parser (no PyGuitarPro), same
          reasoning as J1's hand-rolled MIDI parser: parsers/gp_source.py
          (unzips the .gp container, parses Content/score.gpif, resolves
          its id-indirection graph into a typed GpSource), parsers/
          gp_reader.py (GpReader, mirrors MusicXMLReader/MidiReader) and
          parsers/gp_timeline_builder.py (GpTimelineBuilder, mirrors
          TimelineBuilder/MidiTimelineBuilder). MusicData.__post_init__ and
          workers/score_load_worker.py dispatch on the .gp extension. Covers
          the GP7/GP8 zip+XML container (both share one schema per alphaTab's
          format docs). Adds a synthetic "Chords" voice (GP_CHORD_VOICE_ID)
          for any track with real chord-name/brush data - sticky chord name,
          full chord_pitches for playback, and an explicit (never inferred)
          strum direction - see CLAUDE.md. Pickup detection (Ref 17) and
          repeat/ending/hairpin spans (Ref 29) are out of scope for this
          pass. Ground-truth verified against a real GP8 file (Grateful
          Dead - Ripple, files/GP/) hand-checked note-by-note during
          discovery: first note, sticky chord names/positions across a full
          bar, and the exact 2 explicit Brush-marked beats in the whole
          piece all match.
[ ] J3  Import Braille Music Editor - Ref 25
[ ] J4  Export MusicXML, preserving bars, notes, string and fret data - Ref 26
[ ] J5  Export MIDI - Ref 26
[ ] J6  Export Guitar Pro and Braille Music Editor - Ref 26


###
PHASE K - EDITING MODES                                   (2 tasks)
###
[ ] K1  Ctrl+Shift+E toggles Read-Only / Edit mode with an announcement;
        editing controls locked in Read-Only - Ref 23
[ ] K2  Edit note attributes by typing over a value or stepping it; play the
        updated note immediately - Ref 24


###
PHASE L - VOICE CONTROL                                   (3 tasks)   Ref 19
###
[ ] L1  Spike: Windows SAPI 5.4 / Windows.Media.SpeechRecognition - Ref 19
        > OPENED 2026-08-24. Built a full working implementation against
          classic SAPI 5.4 (SAPI.SpInprocRecognizer via pywin32) on branch
          feature/voice-control, live-tested on the user's real machine.
          Found and fixed several undocumented SAPI/win32com quirks (see
          CLAUDE.md's "Known gaps" entry for the full technical write-up:
          CmdLoadFromFile needed in place of a broken CmdLoadFromMemory, an
          explicitly-wired SpMMAudioIn audio input, winmm-based device
          enumeration since SAPI's own AudioInput token category is empty
          on this Windows build, a CastTo fix for event-callback results) -
          but the underlying engine's own accuracy proved inconsistent even
          for this app's small command vocabulary once every plumbing bug
          was fixed ("stop"/"pause"/"back" reliable, "play"/"preview"/
          others frequently not recognized at all). Abandoned rather than
          shipped - feature/voice-control is preserved (its own commit) for
          reference only, not merged. Windows.Media.SpeechRecognition
          (WinRT) was considered but not attempted, ruled out ahead of time
          in favour of Vosk (see CLAUDE.md for the reasoning). Next attempt
          is Vosk-based, on feature/voice-control-vosk - see L2.
[ ] L2  Command grammar (Tab, Next, Previous, Next Bar, Play Bar...) plus the
        Ctrl+Shift+Enter toggle - Ref 19
        > Now planned against Vosk (offline, open-source, Kaldi-based)
          rather than SAPI 5.4 - see L1's 2026-08-24 follow-up and
          CLAUDE.md's "Known gaps" entry for why.
[ ] L3  macOS speech recognition equivalent - Ref 19, see D-4


###
PHASE M - CROSS-PLATFORM AND PACKAGING                    (4 tasks)
###
[ ] M1  macOS audio driver selection (coreaudio in place of wasapi) and
        keyboard/focus parity with Windows - NFR-01
[x] M2  Windows installer (.exe / .msi) with bundled runtime - NFR-02 AC-02.1/AC-02.3
        > DONE 2026-08-11. New packaging/ (RecallScore.spec, installer.nsi,
          build_installer.ps1) - PyInstaller onedir bundle (PySide6, music21
          minus its unused corpus/matplotlib/PIL, bin/*.dll, soundfonts/
          FluidR3_GM.sf2) wrapped by an NSIS wizard into dist_installer/
          RecallScore-Setup-<version>.exe. Per-machine install (Program
          Files, admin) - confirmed with the user; per-user AppData
          settings (Ref 27) are unaffected since QStandardPaths resolves
          those independent of install location. Real Add/Remove Programs
          entry with a working uninstaller; deliberately leaves the user's
          AppData config alone on uninstall. See CLAUDE.md "Packaging" for
          the full breakdown.
        > FIX 2026-08-11 (live-tested on a Windows 10 VM, first real
          install): "Failed to execute script 'main'" /
          'NoneType' object has no attribute 'write' on launch. ROOT CAUSE:
          console=False (required for a GUI app) means sys.stdout/stderr
          are None, not just closed - this app's print()-based error
          handling (see "Known gaps") crashes the instant anything on that
          path prints, which it did immediately: the VM has no audio
          hardware, so SynthEngine's WASAPI open fails at startup and the
          print() meant to report that warning is what actually crashed.
          Fixed by main.py's _redirect_stdio_if_headless(), called before
          any other import - redirects None stdout/stderr to
          %LOCALAPPDATA%\Recall Score\recall_score.log so every existing
          print() call site keeps working. GOTCHA WORTH REMEMBERING for any
          future --windowed/console=False PyInstaller work.
        > RE-TESTED 2026-08-11 (same Windows 10 VM, after the FIX above):
          uninstalled the crashing build, installed the rebuilt one, app
          now launches without the stdio crash. Confirms the fix, and
          along the way exercised the uninstaller (removing a prior
          install cleanly) for the first time too.
        > Icon (packaging/icon.ico) still not supplied - both the spec and
          main.py's _app_icon_path() degrade gracefully without it.
          Desktop-shortcut checkbox and audio playback on real hardware
          still not live-tested (the VM used so far has no sound card).
[ ] M3  macOS signed .dmg or .app bundle - NFR-02 AC-02.2
[ ] M4  Audit for zero network calls in core features - NFR-07


###
OPEN DESIGN DECISIONS
Mirrors the Discussion Points table in the Product Definition Document.
Add to this as decisions come up; move the outcome into the spec once settled.
###

D-1  Rests in one voice alongside sounding notes in another
     Decided 2026-08-04: an explicit rest in the score is an event with a
     step of "rest" and shows as "rest" in the Note region.
     RESOLVED 2026-08-06 in B2: no special-casing needed. Rests carry a real
     (part_id, staff, voice) since A4/A5, exactly like sounding notes, so
     MusicData._visible_notes() filters a rest the same way it filters any
     other note - a rest in a soloed/active voice shows as "rest" in Region
     3, a rest in a filtered-out voice does not appear at all.

D-2  Global navigation keystrokes vs per-widget handling
     DECIDED 2026-08-06: per-widget, scoped to the Note region, not global.
     Already flagged in the spec's own Discussion Points; the user reversed
     the original lean toward global handling - too confusing and not
     standard Windows behaviour to have arrow keys act application-wide when
     they mean something else in other regions. Ref 4 updated accordingly.
     TimelineListWidget.keyPressEvent already handles Left/Right this way;
     C1/C2/C3 extend the same widget rather than adding a MainWindow-level
     event filter or WindowShortcut context. Revisit only if Tab-ping-ponging
     back to the Note region proves inefficient in practice.
     AMENDED 2026-08-27 (user-requested): the digit-then-Enter bar jump (C4)
     is carved out as global - it's a position-wide jump like Go to Measure,
     not a per-row move, and shares no meaning with digits in other regions.
     Now bare 0-9/Escape WindowShortcuts + the Preview QAction's Enter,
     feeding NavigationController. The ARROW-key family (step event, step
     bar, Home/End, Find Next/Prev) stays per-widget and Note-region-only
     exactly as decided here - those genuinely collide with Region 2's list
     navigation.

D-3  Mechanism for forcing screen reader announcements from PySide6
     Needed for Ref 1 AC4, Ref 3 AC3 and Ref 5. Candidates: QAccessible
     alert events, live-region style updates, or driving the screen reader
     API directly. Decide in D1.

D-4  macOS speech recognition API
     Listed as TBC in the spec. Windows path is SAPI 5.4 /
     Windows.Media.SpeechRecognition. Decide in L3.

D-5  Multi-part GM programs and MIDI channel allocation
     DECIDED 2026-08-06 in A8: one MIDI channel per part (not per staff -
     staves within a part share an instrument in every sample score, and
     per-staff allocation would burn through the 16-channel ceiling faster
     for no observed benefit), assigned in part-list order, skipping
     channel 10 (index 9, percussion), wrapping past 16 parts.
     MusicData.get_channel_for_part() / get_gmidi_program_for_part().

D-6  UK vs US music vocabulary
     RESOLVED 2026-08-10 in F4. Default: UK, unless QLocale.system() resolves
     confidently to a US territory - any other/indeterminate locale stays
     UK. Screen reader announcements: no separate channel needed (Phase D
     was already skipped) - NVDA reads whatever on-screen text the toggle
     produces, so on-screen and spoken wording are automatically the same
     thing. Region 4 attribute names: the internal "measure" key is NEVER
     renamed (storage/menu wiring depend on it staying stable) - only the
     rendered display LABEL changes, via models/vocabulary.attribute_label().
     Surfaces via Options > Terminology Language (a UK/US submenu, not a
     single toggle - see F4's live-tested FIX notes). Scope: bar/measure and
     the duration-name family (crotchet/quaver/etc) only - stave/staff was
     tried and then deliberately excluded, see D-15.

D-7  How tests avoid opening audio hardware
     DECIDED 2026-08-05: constructor injection. MainWindow(synth=None)
     defaults to a real SynthEngine but accepts a stand-in, so tests can
     assert which notes, channel and program would have played - needed by
     Refs 8, 9, 13 and 14. Also makes the engine swappable for the macOS
     driver work in NFR-01.

D-8  Where test sample scores live
     DECIDED 2026-08-05: both. files/ is now tracked in git (66 KB) so the
     real MuseScore exports are backed up and usable as integration
     fixtures, plus hand-written minimal MusicXML under tests/fixtures/ for
     unit tests that need to isolate one behaviour.

D-9  Source of the boundary ("doh") sound - Ref 2 AC4, Ref 3 AC4, see C5
     DECIDED 2026-08-06 in C5: synthesize through the existing SynthEngine -
     a short (quaver-length, ~200ms), quiet, low double-bass note (E2, GM 44
     Contrabass), not tied to the score's own tempo, on a dedicated channel
     (15) reserved from D-5's per-part allocation. No new asset, no
     licensing question (never was going for a literal Homer Simpson
     sample - just a low, unobtrusive indicator that a boundary was hit).
     Making it configurable on/off, and choosing a different instrument/
     pitch, is explicitly deferred until it's actually asked for.

D-10 F6 pane-cycling mechanism and scope - Functional Spec, see C7
     DECIDED 2026-08-06, REVISED 2026-08-06 after live testing: originally
     three panes (menu bar, regions, status bar) - the user's own initial
     spec - but after trying it, the user reversed that: F6/Shift+F6 now
     toggle between just TWO panes, the regions area and the status bar.
     Menu bar access is left to the OS's native Alt mechanism, not F6,
     matching ordinary Windows application behaviour instead of adding a
     bespoke path to something already reachable natively. Mechanism: a
     WindowShortcut-context QShortcut on MainWindow (not
     ApplicationShortcut - see C7's DONE note for why that caused ambiguous-
     shortcut failures) so it fires no matter which child widget holds
     focus. Tab/Shift+Tab keep their existing job of cycling the four
     regions (and, new in C6, cycling a pane's own internal fields for the
     status bar) and stay out of pane-switching entirely. The "regions" stop
     restores whichever region was last focused (tracked via
     QApplication.focusChanged), defaulting to Region 1 before anything has
     been focused - this is a partial answer to the "Focus memory in
     regions" Discussion Point: it remembers the region, not the row within
     it. Since a two-pane toggle has no distinct "reverse", both F6 and
     Shift+F6 call the same _toggle_pane().

D-11 Status bar field tracking and FIFTHS_MAP relocation - Functional Spec,
     see C6
     DECIDED 2026-08-06: key signature (fifths) becomes per-EventSlice state,
     tracked through the score exactly the way A6 already tracks time
     signature (both can change mid-piece, see score_way_to_go) - not a
     separate lookup, not read once at file-open like Region 1's summary
     values. FIFTHS_MAP moves from parsers/musicXML_reader.py to new
     models/key_signatures.py so models/music_data.py can read the current
     position's key without a parsers->models reverse import; Region 1's
     display and the status bar's live display now share one mapping. Status
     bar key wording keeps the existing "/" format ("G major / E minor"),
     not the alternate "or" wording floated during planning - no wording
     change, everywhere stays consistent.

D-12 Region cycle (Tab/Shift+Tab within the MAA) mechanism - Functional
     Spec, see C1/C7
     DECIDED 2026-08-06, after a second live-tested Tab bug (see C7's second
     FIX note): QWidget.setTabOrder cannot reliably close a 4-widget loop at
     all here - not a matter of getting the call order right, a structural
     limitation (relocating any one of the four widgets to fix one edge
     necessarily breaks another, and the four required relocations form a
     genuine cycle of dependencies). Region cycling now bypasses Qt's global
     focus chain entirely: MainWindow.focus_next_region/focus_previous_region
     index into a fixed 4-widget list and call .setFocus() directly, and
     RegionTableWidget/Region2ListWidget/TimelineListWidget's Tab/Backtab
     handlers call those instead of window().focusNextChild()/
     focusPreviousChild(). This is the same pattern C6's status bar fields
     already used for their own internal Tab cycling (StatusBarWidget.
     focus_field(), never routed through the global chain) - now applied
     consistently to the regions area too. No more QWidget.setTabOrder calls
     anywhere in the app.

D-13 Version numbering - Functional Spec, see C8 (About dialog)
     DECIDED 2026-08-06: semantic-ish X.Y.Z in version.py, starting at
     0.1.1, shown in the About dialog. The user owns Y (minor) and X
     (major) bumps and will say when those change; Z (patch) is bumped by
     whoever commits+pushes, on every push. Nothing in the codebase
     automates the bump (no git hook) - it's a manual step to remember when
     asked to commit and push.
     > REVISED 2026-08-11: switched to Year.Release.Build (e.g.
       2026.1.12), on the user's request. Mechanics unchanged, only
       renamed/reinterpreted - Year takes over the old "major" slot
       (calendar year, bumped by the user roughly annually), Release
       keeps "minor"'s semantics (user-owned, bumped when they say so),
       Build keeps "patch"'s semantics (bumped by whoever commits+pushes,
       on every push, never reset when Year or Release changes). No
       packaging script changes needed: build_installer.ps1's version
       regex and its 4-part FILEVERSION padding both already accept any
       dotted numeric string, and the About dialog already reads
       __version__ directly. See version.py's own header comment.

D-14 Metronome click sound synthesis - Ref 14, see E8/E11
     OPEN 2026-08-07: not decided, deliberately parked as functional debt.
     Two attempts tried and rejected live: (1) a synthesized sawtooth wave
     through the existing FluidSynth pipeline (GM "Lead 2", tuned to the
     nearest MIDI notes to the originally-requested 1200Hz/800Hz) - "was
     awful"; (2) GM percussion Claves on channel 10, accent via velocity
     instead of pitch (a percussion voice has no tunable pitch) - "better
     but still not really good enough". Both stayed inside the existing
     in-process FluidSynth engine specifically to avoid opening a second
     concurrent audio stream of uncertain latency alongside its low-latency
     WASAPI session (Ref 9) - that tradeoff needs revisiting if soundfont-
     based synthesis genuinely can't be made to sound right, since the
     remaining options (procedurally generated raw PCM, or user-supplied
     WAV clips - see the original click-generation discussion this session)
     both require a second audio path. See E11 for the open task.

D-15 Stave/staff excluded from F4's UK/US vocabulary toggle
     DECIDED 2026-08-10, live-tested: F4 originally translated stave/staff
     too (Region 2's clef names, Region 4's "stave" attribute), on the
     user's own initial instruction to treat every UK/US term pair the same
     way. After trying it live, the user reversed that: Region 2's clef
     names ("Treble stave", "Bass stave" on Chessel Duet) are the app's own
     generated labels (from detecting the MusicXML clef sign - MusicXML
     itself has no English "stave"/"staff" text, it's <clef><sign>G</sign>),
     not literally read from the file - but the user's underlying call is
     independent of that clarification: keep it simple for now, don't
     translate this family at all, regardless of the terminology-language
     setting. models/vocabulary.py's staff_word()/clef_name() were removed;
     attribute_label("stave", ...) always passes through unchanged, same as
     any other never-translated attribute key. Revisit only if asked for
     again - the removed functions are recoverable via git log on F4's
     commit if so.

D-16 setCurrentRow(row)'s one-arg form does NOT default to NoUpdate
     FIXED 2026-08-10, live-tested regression, found right after F2. F3's
     fix for "a single-note slice has nothing for NVDA to announce after
     selectAll()" (see Selection-driven regions in CLAUDE.md) was written on
     the assumption that QListWidget.setCurrentRow(row) - the plain one-arg
     overload - uses QItemSelectionModel::NoUpdate internally, so it could
     set the current item after selectAll() without touching the selection.
     Confirmed by direct test against this PySide6 build that it does not:
     it collapses the selection down to just `row`. That silently broke
     "moving onto a chord selects and sounds every note in it" - only the
     first note in a multi-note slice stayed selected and sounded. Fixed by
     passing the flag explicitly: setCurrentRow(0,
     QItemSelectionModel.SelectionFlag.NoUpdate) in
     MainWindow._update_timeline_views. Regression test:
     test_navigating_onto_a_chord_selects_and_sounds_every_note in
     tests/test_main_window.py. Lesson for future Qt selection-model code in
     this app: don't trust an overload's assumed default command without
     checking it against the actual PySide6 build in use.

D-17 Up arrow did nothing at the top of a chord's selection
     FIXED 2026-08-11, live-tested regression, found during Phase G testing
     (unrelated to G itself). Landing on a chord selects every note in it,
     current row 0. Down correctly narrowed the selection to row 1 - Qt's
     native ExtendedSelection arrow handling collapses a multi-row selection
     to the new current row, but only as a SIDE EFFECT of the current row
     actually changing. Up at row 0 has nowhere to move, so it silently
     no-op'd and left the whole chord selected instead of narrowing to the
     top note. Fixed in TimelineListWidget.keyPressEvent by explicitly
     clearing selection and reselecting currentItem() after every Up/Down,
     not just relying on the native move to do it - a no-op when the native
     handling already collapsed correctly, and the actual fix at a boundary.
     Regression test:
     test_up_arrow_at_the_top_of_a_chord_collapses_selection_to_the_first_note
     in tests/test_main_window.py.

D-18 Region 2 rebuild clobbered a restored Ref 27 voice filter on reload
     FIXED 2026-08-11, live-tested regression, found testing Phase G (G1).
     MainWindow._on_score_loaded applied a saved ScoreConfig
     (MusicData.apply_config, correctly setting active_voice_filter) BEFORE
     calling _update_ui_regions, which rebuilds Region 2 from scratch via
     load_score_structure - that resets every node back to enabled=True and,
     through the very same filter_changed signal a live Region 2 toggle
     uses, silently overwrote the just-restored filter back to "everything
     on". Net effect: a saved "voice off" toggle (e.g. the user's own repro -
     switching off Viola in Way To Go.mxl) came back on after every reload,
     even though attribute order (which doesn't route through Region 2)
     persisted correctly. Fixed by snapshotting the restored filter before
     _update_ui_regions runs, then handing it to a new
     Region2ListWidget.apply_active_voice_tuples() afterward - restores
     Region 2's own per-node on/off state (inferring a whole part/staff was
     toggled off whenever every voice under it ends up inactive, so the UI
     collapses the same way the original toggle would have) and re-emits
     filter_changed through the normal signal path, the same one a live
     toggle already uses. Regression test:
     test_voice_filter_persists_across_reload_of_same_file in
     tests/test_main_window.py.
     > FOLLOW-UP 2026-08-11 (live-tested, found right after D-18 above): the
       filter itself was restored correctly (Region 2 showed the toggled
       voice off, subsequent navigation excluded it) - but
       _update_ui_regions's own initial audition (play_all=True) fired
       BEFORE apply_active_voice_tuples ran, so the very first sound heard
       on load still included the voice that was supposed to be off (user's
       own repro: hearing both flute and viola on load despite viola being
       correctly shown/enacted as off from that point on). _update_ui_regions
       now takes a play_all param; _on_score_loaded passes play_all=False
       when there's a filter to restore and fires the audition itself
       (_play_selected_region_3_notes) only after
       region_2.apply_active_voice_tuples has run - one correct audition,
       not one wrong one followed by a silent correction. Regression test:
       test_initial_audition_on_reload_respects_the_restored_voice_filter.

D-19 MIDI note gaps and synthetic rests
     DECIDED 2026-08-15: MIDI note gaps (e.g. a quaver note followed by a
     quaver-length silence before the next note) are never backfilled with
     a synthetic rest marker/EventSlice. This matches the existing
     MusicXML behaviour exactly - "Rests are skipped, so navigation lands
     only on attacks" (see Timeline model in CLAUDE.md) - so the two
     formats already agree without any MIDI-specific handling required.
     Considered and rejected: inserting a rest EventSlice so a voice reads
     as "fully populated" bar-by-bar, matching what the notated score would
     show. User's own call, found reviewing files/midi/test1.MID: this
     would add a navigation stop on every silence, reducing usability, for
     a benefit (an explicit "you are on a rest here" cue) the format
     doesn't get any more than MusicXML already provides today. Revisit
     only if real usage shows the gap is actually confusing rather than
     just silent.

D-20 Omit redundant accidentals implied by the key signature (deferred from S6)
     OPEN, raised by the user alongside S6 (2026-08-15), deliberately not
     implemented there. Idea: ordinary notation doesn't re-mark an
     accidental already implied by the key signature - e.g. in G major, the
     F on the staff is understood to be F# without a sharp sign attached to
     every one. Applied to this app's spoken note names, that would mean
     not saying "sharp" for a note whose accidental exactly matches what
     the current key signature already implies, while still saying it in
     full for any other accidental (including a real natural cancelling
     the key - see the correctness point below).

     Needs a real model change first: NoteData only stores the FINISHED
     spoken text (step_name, e.g. "F sharp"), not the raw letter+alter it
     was built from (MusicXML: parsers/timeline_builder.py's
     acc_words-based composition; MIDI: parsers/midi_timeline_builder.py's
     _spell_pitch table) - the omit-logic needs the raw pair to compare
     against the key signature's own altered-degree set (the standard
     circle-of-fifths order: sharp keys alter F,C,G,D,A,E,B in that order;
     flat keys alter B,E,A,D,G,C,F), which a plain string can't reliably be
     parsed back into.

     Correctness wrinkle that makes this a coupled change, not just an
     omission: if the expected F# in G major is spoken as bare "F" (word
     omitted), an actual natural F (a real accidental CANCELLING the key
     signature) would read identically unless it explicitly starts saying
     "F natural" - today's plain alter=0 rendering ("F", no word) is
     already correct and unambiguous specifically because no note's
     spelling depends on key at all yet. Omitting the word for the
     expected case reopens that ambiguity and must be fixed in the same
     pass, not left as a known gap.

     Also open: whether the toggle for this (if built) is session-wide like
     uk_terms (F4 - a personal reading-style preference, independent of
     which score is open) or per-score like metronome_enabled (ScoreConfig)
     - not decided, needs asking when this is picked up. MIDI's existing
     _spell_pitch simplification (sharp-only or flat-only per file, not
     full scale-degree spelling - see its own module comment) composes
     correctly with this idea without further changes: a MIDI note's
     spelled letter+accidental is still checked against the key's real
     altered-degree set the same way a MusicXML note's would be.


###
PHASE S - MIDI IMPORT REVIEW FIXES (2026-08-15)           (7 tasks)
###
  Findings from the user hand-testing J1 (MIDI import) against the fixtures
  in files/midi/ - see the "MIDI Import Review Fixes" plan this phase was
  written from for full detail on each. S1-S4 are direct Ref 25 follow-ups;
  S5 is a general playback bug (not MIDI-specific) surfaced by the same
  review; S6/S7 are new functional requirements not yet in the Product
  Definition Document's numbered table; S8 touches Ref 29 (Performance
  region). One at a time, per the user's own instruction - not a single
  sweep. (Two related design decisions, no code needed for either, are
  recorded as D-19/D-20 above rather than as their own tasks here.)

[x] S1  MIDI title fallback: no title metadata -> use the filename with its
        extension stripped - Ref 25
        > DONE 2026-08-15. parsers/midi_reader.py: credits["Title"] =
          Path(file_path).stem, same "Title" key MusicXML's own
          <credit-type>title</credit-type> capitalizes to.
[x] S2  Region 2 collapses to part-only for a MIDI-loaded score (no
        staff/voice rows - MIDI staff is always fake, voice is untested
        beyond 1 in every real file seen) - Ref 25
        > DONE 2026-08-15. Region2HierarchyModel.build_from_score gained
          collapse_to_parts (default False); get_visible_nodes stops
          descending past a part node when set, but the full 3-level tree
          underneath is untouched - get_active_voice_tuples still finds
          every real voice, so this is purely a rendered-rows change, not a
          filtering change (confirmed by a dedicated test: collapsed rows
          show only 2 part names, but get_active_voice_tuples still returns
          all 5 real (part,staff,voice) tuples). New MusicData.is_midi
          property is what main_window.py's _update_ui_regions passes as
          the flag, replacing what would have been a third copy of the
          .mid/.midi extension check already in music_data.py/
          score_load_worker.py.
[x] S3  Tighten MidiTimelineBuilder's duration-word-match tolerance (0.03 ->
        ~0.01) so a note a couple of percent off a clean fraction falls
        through to the existing numeric fallback instead of being
        mis-named - Ref 25
        > DONE 2026-08-15. tolerance=0.01 in
          parsers/midi_timeline_builder.py's quarter_length_to_display_name
          call. Regression tests: a synthetic 0.98-quarter note now
          resolves duration_name_us to None (falls through to the numeric
          fallback), an exact 1.0-quarter note still resolves to "quarter".
        > FOLLOW-UP 2026-08-15, reported/live-tested against a real
          freehand (non-quantized) MIDI recording the user recorded in
          Reaper (files/midi/test3.MID, added to
          files/midi/readme.md.txt): tightening the tolerance wasn't
          enough on its own - with positions and durations landing
          anywhere, individual notes still frequently landed within 1% of
          SOME type/dots combination purely by numeric coincidence (dense
          coverage: 13 types x up to 4 dots), producing odd names like
          "double-dotted thirty-second" mixed in among plain "sixteenth"s -
          a mix the user called unsatisfactory even where each name was
          individually defensible. Fixed with a per-track second pass
          (MidiTimelineBuilder.build()): models/duration_units.py gained
          is_simple_duration_match() (whole/half/quarter/eighth/16th/32nd
          with 0-1 dots only), and any track where over 5% of its notes'
          duration names are NOT simple has every one of its notes' - not
          just the weird ones - duration_name_us reset to None, so the
          whole track is consistently numeric rather than a mix. Verified
          against test3.MID directly: 3 of 16 notes matched something (2
          weird, 1 "sixteenth"), 18.75% > 5%, all 16 now numeric.
          Also fixed in the same pass: Region 3's inline note text omits
          the "duration" label only when duration_name_us is a real word
          (per an earlier user request - "quaver" is self-explanatory) -
          the numeric fallback now keeps the label ("duration 1.5"), since
          a bare number alone in the comma list could be mistaken for
          anything. models/music_data.py's _format_note_for_region_3.
[x] S4  Fix mixer volume/pan leaking across file switches: apply_mixer only
        sends CC for parts with an explicit override, so a channel's value
        from the PREVIOUS score (the synth is a long-lived singleton)
        survives into a new one with nothing to reset it. Reset every
        part's channel to the GM default (volume 100, pan 64) before
        applying this score's overrides.
        > DONE 2026-08-15. controllers/playback_controller.py's apply_mixer
          now resends every mixer-controllable channel unconditionally -
          override if one exists, else mixer_settings.DEFAULT_VOLUME /
          default_pan_for(key) - the same revert values cancel_mixer_edit
          already used, just now also applied on every load, not only on
          Mixer-dialog cancel. Regression test:
          test_switching_files_does_not_leak_a_mixer_override_onto_the_new_score
          in tests/test_main_window.py, reproducing the user's own repro
          (two unrelated scores whose first part shares a channel).
[x] S5  Per-part instrument (GM program) override + display-name override,
        new dialog, for both MusicXML and MIDI parts, persisted per-file
        (ScoreConfig). Needs a new 128-entry GM instrument name table
        (doesn't exist anywhere yet) - land that plus the quick win of
        suggesting a MIDI track's name from its own Program Change event
        (instead of "Track N") first, independently.
        > DONE 2026-08-15. models/gm_instruments.py: the 128-name GM Level 1
          table (1-indexed, matching PartStructureInfo.gmidi_program's own
          convention), plus gm_instrument_name()/gm_program_for_name().
          parsers/midi_reader.py's _build_parts_info uses it for the quick
          win - an untitled track with a Program Change now suggests the GM
          name instead of "Track N" (doesn't fix BluePeter.mid itself,
          whose tracks DO carry literal names like "Track1" per the user's
          own description - a real name always wins over the suggestion;
          the dialog below is the actual fix for that file).
          widgets/instrument_dialog.py: one row per part (QListWidget, same
          shape as MixerDialog), a plain QLineEdit for the display name and
          an editable QComboBox + QCompleter (MatchContains, case
          insensitive) for the instrument - per the user's explicit design
          ask: name field + combo box, program numbers never surfaced, some
          quick search facility, simple UI, OK/Cancel. No live audio
          preview (unlike Mixer) - a name/program change has nothing to
          preview live, and the user asked to keep this simple. Edits are
          committed on row-switch and again in overrides() itself (covers
          OK on whatever row was on screen without switching away first);
          overrides() returns only parts that actually changed.
          MusicData.apply_part_overrides(name_overrides, program_overrides)
          mutates parts_info in place (get_gmidi_program_for_part already
          reads it live, so the program override needed no other wiring)
          and re-syncs NoteData.part_name for every affected note - R5's
          "two copies of a part name must agree verbatim" invariant
          (CLAUDE.md) applies here too: get_performance_report_lines joins
          parts_info.name against note.part_name by exact text match, so a
          rename that touched only one side would silently show 0 notes
          again, the same bug class R5 fixed. Region 2's part-row label is
          updated in place (Region2HierarchyModel.rename_part / Region2List
          Widget.rename_part), never via load_score_structure - that
          resets every node back to enabled=True, which would silently
          re-enable whatever the user had switched off. ScoreConfig gained
          part_name_overrides/part_program_overrides (Dict[part_id, ...]),
          same "explicit overrides only" shape as mixer, wired through
          export_config/apply_config (best-effort: an override for a
          part_id no longer in the score is dropped) and persistence/
          score_config.py's JSON codec. Edit menu > "Instruments..."
          (Ctrl+Shift+I). 32 new tests across tests/models/
          test_gm_instruments.py, tests/models/test_music_data.py, tests/
          parsers/test_midi_reader.py, tests/widgets/
          test_instrument_dialog.py and tests/test_main_window.py
          (including that a rename survives a reload, and that it does not
          reset Region 2's on/off state).
[x] S6  Key signature override, user-settable, persisted per-file - own
        dialog (see FOLLOW-UP below - not S5's, despite the original
        sketch). v1 scope: one whole-piece override, not a per-position
        list.
        > DONE 2026-08-15. One combo box, not two separate fifths/mode
          controls: models/key_signatures.py's key_override_options() lists
          all 15 major + 15 minor key names (plus a "use the file's own
          key" sentinel, 31 entries) - picking "G major" sets fifths and
          major/minor display mode together, which is also what fixes the
          weak "C major / A minor" label the user flagged (
          key_signature_display_name(fifths, mode) splits FIFTHS_MAP's own
          string rather than adding a second table). Non-editable combo,
          deliberately unlike the instrument one - 31 entries is few enough
          that native Qt keyboard-search (press a letter, jump/cycle) is
          sufficient alone, and it avoids the free-text-resolution handling
          the instrument field needs.
          MusicData.apply_key_signature_override(fifths, mode): for a
          MIDI-loaded score, re-spells every note via
          parsers/midi_timeline_builder.py's _spell_pitch - MIDI has no
          real notation to derive spelling from, so a wrong/missing file
          key produces wrong enharmonic spelling until corrected here.
          Symmetric by design: clearing the override (fifths=None)
          re-derives each note's spelling from its own new NoteData.
          file_key_fifths field (the fifths MidiTimelineBuilder actually
          spelled it against at parse time) rather than a separate cached
          "original text", so no re-parse and no distinct restore path are
          needed. MusicXML notes are never re-spelled - their spelling
          comes straight from the file's own <step>/<alter> and never
          depended on key; the override only ever changes what key is
          DISPLAYED for an XML score (get_region_1_data/
          get_status_bar_fields, same live-rebuild pattern the Tempo field
          already used for the UK/US toggle). ScoreConfig gained
          key_signature_override_fifths/_mode, applied unconditionally in
          apply_config (unlike the part overrides, not tied to score
          content that could go missing on reload).
        > FOLLOW-UP 2026-08-15 (user tested, two changes): (1) the key
          combo initially lived as a second control inside S5's Instruments
          dialog - the user found renaming/reprogramming a part and
          overriding the score's key too different a pair of actions to
          share one dialog. Split into its own widgets/key_signature_dialog.py
          + Edit > "Key Signature..." (Ctrl+Shift+K); widgets/
          instrument_dialog.py reverted to exactly its S5 shape.
          (2) Reported, live-tested: NVDA read "Bb major" as just "b major"
          and "F# minor" lost the "#" entirely - FIFTHS_MAP had never
          picked up this app's own spoken-friendly convention for
          accidentals (CLAUDE.md: "F sharp", never a symbol). Fixed at the
          source - FIFTHS_MAP's strings themselves now spell "F sharp"/
          "B flat" etc. in full, which key_signature_display_name/
          key_override_options both build from, so every display site
          (Region 1, status bar, the new dialog) picked up the fix with no
          further changes needed. 21 tests total across tests/models/
          test_key_signatures.py, tests/models/test_music_data.py, tests/
          widgets/test_key_signature_dialog.py and tests/test_main_window.py.
        > FOLLOW-UP: see D-20 below for the "omit the accidental word when
          it's just what the key signature already implies" idea the user
          raised alongside this, deliberately deferred out of S6's scope.
[x] S7  Region 5 + audio cue: one-shot alerts for time-signature changes and
        immediate/point tempo changes (both already parsed as
        TempoChange/EventSlice.time_sig, for MusicXML and MIDI) - Ref 29.
        Accelerando/ritardando (a span, like a hairpin) is explicitly out
        of scope - nothing parses it yet and it needs investigation into
        how the user's scores actually encode it before it can be designed.
        > DONE 2026-08-15. No new data model needed for time signature -
          EventSlice.time_sig was already tracked correctly per slice by
          both builders (mid-piece changes, for the status bar's "Time:"
          field); get_performance_region_rows (models/music_data.py) just
          compares it against the immediately preceding slice in whichever
          list the cursor's slice came from (self.timeline_slices, so this
          works whether or not the metronome's synthetic beat markers are
          currently spliced in). Tempo reuses the existing
          _tempo_change_at(index) lookup the status bar/Sequencer already
          have (comparing the raw (bpm, beat_unit_ql, beat_unit_name)
          tuple, not the divided display float, to avoid any float
          precision false-positive) - deliberately NOT
          effective_tempo_bpm/effective_tempo_display_bpm, which fold in
          the user's live F/S/D playback offset (Ref 12): the alert is
          about what the SCORE says, so nudging the offset must never
          itself look like a tempo change. Never fires at the very first
          slice (index 0) - the opening time signature/tempo is already
          shown in Region 1 and the status bar every load; alerting on it
          in Region 5 too would just be noise. Reuses the existing hairpin
          jump-target mechanism as-is: both rows set jump_target_quarters
          to the row's own position, so Region 5's Ctrl+Home/Ctrl+End both
          resolve to it (a harmless no-op jump, since the alert only ever
          shows where the cursor already is) - no new navigation code
          needed. The existing diff-based performance-cue trigger
          (RegionPresenter.refresh_region_5) picks this up for free, same
          as the three existing span kinds. Verified against real files:
          midi_test2 (4/4->3/4 AND 120->80bpm together at bar 9) produces
          both rows on the same slice, in that order. 6 new tests across
          tests/models/test_timeline_characterisation.py and
          tests/test_main_window.py.


###
PHASE R - CODE REVIEW REMEDIATION (2026-08-14)           (18 tasks)
###
  Findings from a full senior Python/PySide6 review of the codebase at
  commit ab860ae. Full detail - evidence, measurements, suggested fixes -
  is in code_review_2026-08-14.md at the repo root; the R-numbers here are
  that document's own numbering. Not requirement-driven (no Ref numbers):
  this is code health, so nothing here appears in the COVERAGE CHECK below.
  Review scores: performance 8.5/10, simplification 6/10, readability
  6.5/10, architecture 7.5/10, technical debt 8/10, tests 9/10.

HIGH
[x] R1  Region 2/3 Tab handlers are dead code - QAbstractItemView eats Tab at
        event() level before keyPressEvent, as region5_list_widget.py already
        documents. The focus cycle currently works only because Qt's native
        chain coincidentally matches creation order.
        > DONE 2026-08-14. Extracted the Region 5 event()-level interception
          into widgets/region_focus_cycle.py (RegionFocusCycleMixin), applied
          to all four QAbstractItemView-based region widgets. Both Shift+Tab
          conventions (Key_Backtab, and Key_Tab+ShiftModifier) checked in one
          place. Tests assert the handler itself fires, not just that focus
          happened to land right - the old coincidence would pass a
          focus-only assertion.
[x] R2  models/ transitively imports PySide6 (music_data -> score_config ->
        QStandardPaths), breaking the invariant main_window.py:52 relies on.
        > DONE 2026-08-14. ScoreConfig dataclass + the VoiceKey/StaffKey
          aliases moved to models/score_config_data.py (stdlib only);
          persistence/score_config.py keeps the Qt-aware I/O AND the JSON
          key codecs (how a tuple key is spelled in a file is a
          serialisation concern, not part of the data shape) and re-exports
          ScoreConfig, so every existing import site is unchanged.
          test_models_package_does_not_import_qt in tests/test_harness.py
          guards it - runs in a subprocess, since this test session already
          has PySide6 loaded and in-process sys.modules can't attribute it.
          Verified meaningful, not vacuous: importing models/* leaves 0
          PySide6 modules loaded, importing persistence.score_config leaves 15.

MEDIUM
[x] R3  QApplication.focusChanged connected in setup_ui, never disconnected -
        destroyed windows keep receiving it (main_window.py:227)
        > DONE 2026-08-14. _disconnect_focus_tracking(), called from
          closeEvent, guarded by a _focus_tracking_connected flag rather than
          try/except: closeEvent legitimately runs twice (an explicit close()
          then fixture teardown, as two tests do) and PySide6 does NOT raise
          on a stale disconnect - it emits a RuntimeWarning and carries on,
          so an except clause would never have caught it. The suite now runs
          clean under -W error::RuntimeWarning.
[x] R4  TimelineBuilder: identical measure-number parse block x5, and four
        separate _MeasureOffsetWalker passes over the first part
        > DONE 2026-08-14. _measure_number()/_raw_measure_number() module
          helpers replace all five hand-written copies. _measure_start_quarters,
          _tempo_changes, _repeat_and_ending_spans and _hairpin_spans collapse
          into one _scan_first_part() returning a _FirstPartScan, with
          _step_barline/_step_wedge as per-element helpers (-148 lines).
          Verified behaviour-preserving by diffing slices, tempo_changes,
          repeat/ending/hairpin spans, beat_markers and total_measures between
          the pre- and post-refactor parsers across all 44 scores in files/,
          examples/ and tests/fixtures/: zero differences.
[x] R5  <part-name> read independently by MusicXMLReader and TimelineBuilder
        and required to match verbatim - TimelineBuilder already receives
        parts_info and can derive it instead of re-reading
        > DONE 2026-08-14. TimelineBuilder._part_names() derives from
          parts_info when present, keeping the etree read only for the
          no-reader path (MusicData(file_path=...) built directly, which has
          no parts_info). Agreement is now structural rather than a convention
          to remember. Two regression tests: one hands TimelineBuilder a
          parts_info the file itself contradicts (proving the XML is no longer
          consulted), one covers the fallback. Re-checked end to end on the
          Korean-<part-name> bourree - the report shows 492 notes, not 0.
[x] R6  MainWindow is a 1,469-line God object - split MenuBuilder /
        PlaybackController / RegionRefresher; three duplicated
        selectedIndexes idioms; nine near-identical navigation one-liners
        > DONE 2026-08-14. New controllers/ package - ScoreSession (what is
          loaded + the load thread), PlaybackController (transport, tempo,
          cues, toggles, mute, mixer), NavigationController (cursor movement
          only), RegionPresenter (the only widget-touching controller; owns
          the refresh choke point), AttributeController, FocusController,
          ScorePersistenceController - plus widgets/menu_builder.py, which
          returns an Actions dataclass the shell unpacks.
          main_window.py 1320 -> 682 lines, 89 -> mostly one-line delegators
          and read-only properties, kept deliberately as the facade the
          region widgets call through window() and the tests drive.
          Controllers read session.music_data per call and never cache it,
          since MusicData is replaced wholesale on each load.
          Also landed inert groundwork for the wishlist's top audio items:
          models/mixer_settings.py (#4 mixer, #7 mute) persisted in
          ScoreConfig, SynthEngine.set_channel_volume/pan, and Region 2 solo
          (#8). No UI for any of it yet - all default-inert, verified rather
          than assumed (empty mixer sends 0 CC; nothing-soloed takes the
          untouched original code path).
          Evidence: the 358 pre-existing tests pass UNCHANGED (git diff on
          tests/ is empty; the 12 new tests are in new files), plus a 44-score
          behaviour baseline - playback events, region 3/4 text, status
          fields, performance rows, report lines and Region 2 voice filtering
          at every slice - captured before and after and byte-identical. An
          offscreen end-to-end smoke run exercised load, navigation, all
          three transport states, toggles, the Tab cycle wrap, F6, Region 5
          jumps and the dialect switch.
[x] R7  Stale docs: metronome.py/position_announcer.py still describe the
        removed .sf2.json sidecar; music_data.py:1150 says two reserved
        channels when there are three
        > DONE 2026-08-14. Both docstrings now state the real mechanism (a
          one-shot sample retires itself; no note-off scheduling anywhere) and
          say explicitly that the sidecar description outlived the sidecar.
          The remaining .sf2.json mentions in synth_engine.py and
          tools/wav_to_sf2.py are correct - they document the removal.
[x] R8  create_property_list's items parameter and population loop are dead -
        both call sites pass []
        > DONE 2026-08-14. Parameter and loop removed, both call sites
          updated. Population is _populate_table's job and always was.
[x] R9  Prose-to-code ratio 0.56 - keep the invariants in the code, move the
        bug-investigation narratives to tasks.txt/commit messages
        > DONE 2026-08-14. 608 prose lines removed across 22 files; ratio
          0.60 -> 0.44 (the 0.56 in the original finding counted tools/ too).
          Biggest: main_window.py -176, music_data.py -143, synth_engine.py
          -85, timeline_builder.py -48, sequencer.py -35.
          The rule applied: keep WHAT the constraint is and WHY the code must
          be this way (non-obvious Qt/FluidSynth/music21 behaviour, invariants
          that would be re-broken if unstated); cut HOW it was found - "reported
          bug, live-tested", the user's repro anecdote, what the old code did,
          which test caught it. git log and this file already hold that.
          Applied to the R1-R18 annotations added earlier the same day too;
          they were the same kind of commentary.
          Verified prose-only by an AST guard: every touched file's tree, with
          docstrings blanked, is byte-identical to its committed version (a
          comment never reaches the AST at all). 358 tests pass, pyflakes
          clean, clean under -W error::RuntimeWarning. Spot-checked that the
          load-bearing gotchas all survived - the samplerate-must-be-a-
          constructor-arg one, setCurrentRow's NoUpdate flag, QAbstractItemView
          eating Tab in event(), FluidSynth releasing by channel+key, one-shot
          samples retiring themselves, the checked=False lambda arg, pickup
          beat positioning, findall-not-find, ExtendedSelection's boundary
          no-op, active_voice_filter's None-vs-empty-set distinction, and the
          dataclass field-ordering constraint on attribute_order.

LOW
[x] R10 _slice_is_navigable: latent IndexError for an out-of-range index with
        the metronome on (music_data.py:260)
        > DONE 2026-08-14. Given its own bounds check -
          _slice_has_visible_notes returns False for both "out of range" and
          "in range but nothing visible", so the guard could never have been
          inherited from it.
[x] R11 Region2ListWidget stores node_id via setData and never reads it back;
        also the codebase's only unscoped Qt.UserRole
        > DONE 2026-08-14. Removed; current_node() resolves a row through
          _current_visible_nodes, which is built from the same list in the
          same order.
[x] R12 Unconditional [DEBUG] prints on the production load path
        (musicXML_reader.py:22)
        > DONE 2026-08-14. Both removed. In a release build stdout is the
          log file under %LOCALAPPDATA% (main.py's
          _redirect_stdio_if_headless) - the one place a real error report
          has to stay legible.
[x] R13 load_score_from_file silently no-ops while a load is in flight (no
        cue at all for a screen-reader user); ScoreLoadThread instances are
        parented and never deleteLater'd
        > DONE 2026-08-14. Split into its two halves; only the second was
          worth fixing.
          NO CUE, deliberately (the user's call, and the measurements agree).
          The guard sits at the top of load_score_from_file, whose only
          production caller is _open_score_file_dialog - i.e. it runs AFTER a
          modal QFileDialog returns. Hitting it would mean completing a whole
          second file-picker interaction inside the previous load's window.
          Measured load times: 12-80ms typical, 390ms worst (Pachelbel's
          Canon, 3856 notes), 217ms for the Chopin etude. Not reachable by
          hand, so an audible cue would be dead code. Revisit ONLY if a
          non-dialog load path appears (recent-files list, drag-and-drop, a
          command-line argument), where two loads could fire back to back.
          FIXED: ScoreSession._on_thread_finished now calls deleteLater() on
          the finished thread. It is parented to the session, so the C++
          object outlived the Python one and accumulated one per file opened
          for the life of the process. Small - the parsed score is a local
          inside run(), so nothing large was retained - but free to fix.
          Verified: 8 consecutive loads leave 0 QThread children alive.
[x] R14 MusicData.__post_init__ sets undeclared attributes on a dataclass -
        attribute_order is public API missing from the field list
        > DONE 2026-08-14. attribute_order is now a declared field
          (default_factory=list, since DISPLAY_ATTRIBUTE_ORDER isn't bound yet
          at field-definition time); __post_init__ fills it only when empty,
          so a caller-supplied order is honoured. The private caches
          (_beat_markers, _real_timeline_slices, the visibility caches) stay
          undeclared deliberately - internal, not API.
[x] R15 set_field(4, ...) hardcodes the playback status field index
        > DONE 2026-08-14. StatusBarWidget exposes named field indices
          (POSITION_FIELD .. POSITION_ANNOUNCER_FIELD); MainWindow uses
          PLAYBACK_FIELD.
[x] R16 SynthEngine._stop_group's list.remove releases the first matching
        (channel, note) - a unison across two groups cuts the wrong one
        > DONE 2026-08-14. Reference-counted: the group's claim is removed
          from _active_notes, and noteoff is sent only once no other live
          group still holds that (channel, note). New tests/audio/
          test_synth_engine.py covers the unison case, the ordinary case and
          the no-engine path - instances built via object.__new__ with a
          recording stand-in for _fs, since conftest blocks real engine
          construction (D-7).
[x] R17 _measure_start_quarters reads only the first direct-child <attributes>
        per measure; build()'s walker applies them anywhere
        > DONE 2026-08-14, as part of R4's consolidation. Measure LENGTH is
          computed from the signature snapshotted at the measure's first
          <attributes> (a time signature change takes effect at a barline);
          every other consumer reads the walker's live value, so a mid-measure
          <attributes> still affects offsets after it. Same behaviour as
          before, but now one explicit decision instead of two methods
          accidentally disagreeing.
[x] R18 latency_harness.py unused local 'app'; score_load_worker.py cites
        improvements.txt, which is not in the repo
        > DONE 2026-08-14. The QCoreApplication must be held (its QTimers
          and the jitter measurement's QEventLoop depend on it), so it is now
          asserted against QCoreApplication.instance() rather than left as a
          bare unused local. Stale improvements.txt reference dropped. Also
          removed an unused pytest import in test_timeline_characterisation.py
          - pyflakes is now completely clean across the project.


###
PHASE CF - COMPREHENSIVE FIND (2026-08-28)                (7 phases)
###
  "Anything in the score that is not a plain note, a rest or a lyric
  should be findable." Written from find_feature_plan.md after auditing
  every MusicXML file in files/ + examples/ against the MusicXML 4.0
  element reference. See that plan for the full design (D1-D16) and the
  per-phase edit log; each phase is behaviour-additive and gated by the
  tests/manual/ fingerprint harnesses (§7 of the plan).

[x] CF-P0  Value-level Find infrastructure - FindTarget.value,
           VALUE_EXPANDED_KEYS allow-list (D2), per-value dialog rows,
           occurrence counts (D13), (key, value) cache, "Filter:" box.
           > DONE 2026-08-28. 1044 tests pass.
[x] CF-P1  Note-attached notations - tie, slur, tuplet, fermata, arpeggio
           (chord notes only, D7), cautionary/editorial accidental (D14),
           tier-2 technical marks -> technique, glissando/slide, grace
           summary, and the D6 <notations> catch-all (other_notation).
           > DONE 2026-08-28. 1062 tests pass; fingerprint gate clean
             (only new find targets + Region 4 rows).
[x] CF-P2  Chord symbols + chord diagrams - chord_symbol (findable where
           the core `step` key is not) across MusicXML <harmony>, UG [ch]
           and GP chord names; chord_diagram from harmony/frame.
           > DONE 2026-08-28. 1068 tests pass.
[x] CF-P3  Direction spans + points (D5 per-part, D15 no Region 5 for
           pedal/octave-shift) - DirectionSpan/DirectionMark for pedal,
           octave-shift, rehearsal, dashes, bracket + the D6
           <direction-type> catch-all (other_direction).
           > DONE 2026-08-28. 1082 tests pass.
[x] CF-P4  Barlines, clefs, measure styles - BarlineMark (double/other,
           never the final light-heavy, D16), ClefChangeMark,
           MeasureStyleMark.
           > DONE 2026-08-28. 1108 tests pass.
[x] CF-P5  Cross-format consistency - GP's `tied`/`slide` note flags move
           onto the MusicXML `tie`/`glissando` keys so Find behaves the
           same on a .gp and a .mxl file; `muted` stays in `articulation`.
           > DONE 2026-08-28. 1109 tests pass; fingerprint gate clean
             (diff confined to the one .gp file: articulation -> tie /
             glissando, plus its downstream Find targets and Region 4 text).
[x] CF-P6  Documentation - user_guide.md §5.7 (value-level Find, occurrence
           counts, the attribute-vs-marking count asymmetry, catch-alls),
           user_guide.html regenerated, CLAUDE.md "Comprehensive Find"
           architecture note + rewritten ornaments Known-gaps entry, this
           tasks.txt block.
           > DONE 2026-08-28.


###
UG RETHINK - getting the most out of an Ultimate Guitar page (2026-08-30)
Plan: UGRethink.txt. Investigated the 18 example URLs; no time signature is
ever present, sections are always present, 6 of 17 tabs carry 2-3 strum
patterns, and the app knew only 3 of UG's 9 strum codes (mislabelling 202,
the commonest, as "muted strum" - it is a pause).

[x] UG-P1  models/strum_codes.py: full 9-code STRUM_CODES table + slot_words
           (from UG's own bundle). models/strum_pattern.py (new): one parsed
           pattern + slot_ms/bar_count/slot_labels/slot_rows.
           models/beat_position_words.py (new): the beat-position word
           vocabulary, extracted from audio/position_announcer.py (re-exports).
           ug_source.py: read ALL of tab_view.strummings into
           UgSource.strum_patterns; add capo; FORMAT_VERSION 2 with v1 migrate.
           > DONE 2026-08-30.
[x] UG-P2  models/section_span.py (new) + section_spans through
           timeline_build.py. UgTimelineBuilder populates it from [Section]
           labels. Region 5 row pair, Find "section" marking kind,
           Ctrl+Alt+Left/Right (NavigationController.next/previous_section),
           Performance Report block.
           > DONE 2026-08-30.
[x] UG-P3  widgets/strumming_dialog.py (new): Edit > Strumming Patterns...
           (Ctrl+Shift+U) - flat one-row-per-slot list + Alt+P looped demo
           via SynthEngine.play_strum_pattern. build_strum_schedule
           generalised to per-slot ms + effects. Per-chord strummed audition
           removed (ug_strum_pattern, play_strummed_bar, sound_events is_ug
           branch).
           > DONE 2026-08-30.
[x] UG-P4  ASCII tablature blocks skipped from chord/lyric alignment and
           counted ("Tablature blocks: N (not imported)" credit); capo and
           multi-pattern summary credits.
           > DONE 2026-08-30.
[x] UG-P5  File > Recent Files: "<filename> (<folder>)", & escaped.
           > DONE 2026-08-30.
[x] UG-P6  Docs: user_guide.md 10.2/10.3/10.7/10.8 + shortcut tables,
           user_guide.html regenerated, CLAUDE.md UG architecture entry,
           this block. version.txt 2026.1.43 -> 2026.1.44. 1178 tests pass;
           both fingerprint harnesses MATCH.
           > DONE 2026-08-30.


###
COVERAGE CHECK - every requirement appears at least once
  Ref  1 D2        Ref  8 A8,B4     Ref 15 F1,F2     Ref 22 H3,H4
  Ref  2 C5        Ref  9 A8,E10    Ref 16 A5,F3     Ref 23 K1
  Ref  3 C2,D3     Ref 10 E5        Ref 17 A3        Ref 24 K2
  Ref  4 C1        Ref 11 E6        Ref 18 A2,A6,A9  Ref 25 I1,J1,J2,J3
  Ref  5 C3,D3     Ref 12 E1,E2,E3  Ref 19 L1,L2,L3  Ref 26 J4,J5,J6
  Ref  6 C4,D3     Ref 13 E7        Ref 20 H1        Ref 27 G1,G2,E12
  Ref  7 B1-B5     Ref 14 E8,E9,E11 Ref 21 H2        Ref 28 E12

  NFR-01 M1     NFR-02 M2,M3     NFR-03 A1,A2,A10    NFR-04 E10
  NFR-05 D4     NFR-06 I1,I2     NFR-07 M4

  Reported bugs: tempo units A9, playback instrument A8
  Functional debt: metronome click sound quality E8/E11, see D-14
  Discussion Points: nav keys scoped to Note region (RESOLVED) D-2/C1,
                     UK-US vocabulary (RESOLVED) D-6/F4
###
