###
RECALL SCORE - DEVELOPMENT TASK CATALOGUE
Derived from "Product Definition Document.md"   |   Created 2026-08-04
###

HOW TO USE THIS FILE
  - One line per task. Mark progress by changing [ ] to [x], or [~] for
    in progress. Add notes under a task with a "  > " prefix.
  - "Ref N" points at the numbered functional requirements table in the
    Product Definition Document; "NFR-N" at the non-functional table.
    Go and read the acceptance criteria there before starting a task.
  - Near-term phases (A-C) carry detail. Later phases are deliberately one
    line each - their detail depends on decisions not yet made. Expand them
    when we get there.
  - This file is tracked in git (since 2026-08-05) so it is backed up.
    Commit it along with the code changes each task produces.
  - Completed tasks' notes were spring-cleaned 2026-08-07 down to essential
    outcomes and hard-won gotchas (root causes, non-obvious library
    behaviour) - file-by-file mechanism, test names/counts, and anything
    else recoverable via `git show`/`git log` on the task's own commit were
    cut. Unstarted/partial tasks were left untouched.

PHASE ORDER RATIONALE
  Phase A comes first because four defects in the timeline model gate large
  parts of the spec: notes are not attributed to their real part (blocks
  Ref 7), rests are discarded (blocks Refs 14 and 16), and divisions and
  time signature are read once from the first match (breaks Ref 18 on any
  score that changes either mid-piece). Building playback or filtering on
  top of these means reworking that code later.

  A2 lands before A3-A5 on purpose. Those three all rewrite
  _build_timeline_from_xml, and Refs 17 and 18 are correct today. The
  characterisation tests are what prove they are still correct afterwards.

CURRENT STATE
  Done      : Ref 18 (TS-relative beat units - verified on 6/8), Ref 7
              (Region 2 filtering - parts/staves/voices toggle via Region
              2's O key, see B1-B4)
  Broken    : Ref 17 - pickup numbering is wrong on scores MuseScore exports
              with number="0" implicit="yes" (see A3)
  Partial   : Ref 1 (no announcements), Ref 2 (Region 3 only, no boundary
              cue), Refs 8/9 (audition fires, latency unmeasured, wrong
              instrument), Ref 16 (no rests), Ref 20 (string/fret only),
              Ref 25 (MusicXML only)

TEST SCORES (files/)
  Use these real MuseScore exports for functional/integration testing - each
  exercises a different set of requirements no hand-written fixture covers
  on its own. See tests/conftest.py for the score_* fixtures that expose
  them (score_bourree, score_duet, score_way_to_go, score_long_tune).
  bach-bourree-tab short  4/4, guitar, notation staff + TAB staff duplicating
                          it, pickup exported as number="1"
  Chessel Duet            6/8, TWO parts (Piano GM 1 with 2 staves + voices
                          1/5/6, Classical Guitar GM 25 with voices 1/2),
                          pickup exported as number="0" implicit="yes",
                          tempo eighth=96. Exercises multi-part, compound
                          time and the second pickup convention - it is
                          currently mis-parsed in several ways.
  Way To Go               4/4, TWO parts (Flute GM 74, Viola GM 42), no
                          pickup. Time signature changes 4/4 -> 3/4 -> 4/4
                          and key signature changes mid-piece (fifths 1 -> 2
                          -> 1). The score to use for A6 (divisions/time
                          signature tracked as they change, not first-match).
  long tune               4/4, one part, 130 measures on paper (real notes
                          end around measure 112 - measures 113-130 are
                          trailing empty bars padding the final system, the
                          same rest-only-tail pattern _sounding_bounds
                          excludes elsewhere, see C5). Provided by the user
                          for C4 (digit-entry bar jump, Ref 6), to exercise a
                          real multi-digit measure number end to end.


###
PHASE A - FOUNDATIONS AND MULTI-PART CORRECTNESS         (10 tasks)
###
[x] A1  Test harness: pytest + pytest-qt, tests/ tree, requirements files
        - NFR-03 AC-03.3
        > DONE 2026-08-05. Offscreen Qt, audio blocked via injectable synth
          (D-7), requirements split runtime/dev (mido dropped - unused since
          the FluidSynth switch). MusicData(file_path=...) tests bypass
          music21 for speed (~1ms vs ~460ms via MusicXMLReader.load()) - see
          CLAUDE.md for the full harness description.

[x] A2  Characterisation tests for the current parser - NFR-03, locks Ref 18
        > DONE 2026-08-05. Locked in ts-relative duration units and chord/
          string-fret/slice-ordering behaviour before A3-A6 rewrote the
          timeline builder, so regressions would show up as test failures.
          Recorded the known-bad pickup numbers as xfail, flipped in A3.

[x] A3  FIX pickup numbering when the file already numbers the pickup 0
        - Ref 17, currently broken
        > DONE 2026-08-06. Two MusicXML pickup conventions exist: number="1"
          (needs re-indexing down by one) and number="0" implicit="yes"
          (already correct - re-indexing it again produced measure -1 and
          corrupted beat positions). Fix only re-indexes when
          first_measure_number != 0.

[x] A4  Add part_id to NoteData; stamp each note with its real <part> id and
        that part's name - prerequisite for Ref 7
        > DONE 2026-08-06. Every note was labelled with parts_info[0]'s name,
          and (staff, voice) collided across parts with no part_id to
          disambiguate. NoteData/PartStructureInfo gained part_id; the
          timeline builder now stamps each note from its own <part>.

[x] A5  Explicit rests become timeline events - Ref 16 AC2
        > DONE 2026-08-06. NoteData.octave/midi_pitch are Optional[int]=None
          for a "rest" step. get_midi_notes_for_indices/
          get_region_4_data_for_indices guard on None so playback stays
          silent and Region 4 omits octave/midi rows. <forward>/<backup>
          gaps do NOT become events - only a literal <note><rest/>.

[x] A6  Track divisions and time signature as they change through the score
        instead of first-match - Ref 18
        > DONE 2026-08-06. .//attributes/time and .//attributes/divisions
          only ever matched the FIRST occurrence in the file. Replaced with
          apply_attributes(), re-applied every time an <attributes> element
          is walked (state resets per part - divisions is declared per-part
          in MusicXML), so ts_duration reflects the measure a note is
          actually in. Verified on Way To Go (4/4->3/4->4/4 mid-piece).

[x] A7  Measure index helpers over timeline_slices - prerequisite for Refs 3, 5, 6
        > DONE 2026-08-06. first_event_index_of_measure(n) returns None for
          an empty measure (used by C2/C4 for "unknown bar" handling instead
          of raising); last_event_index() returns -1 on an empty timeline.

[x] A8  FIX playback instrument per part/voice instead of always parts_info[0]
        - Ref 8, Ref 9 AC2, reported bug. Depends on A4.
        > DONE 2026-08-06. get_current_gmidi_program() always returned the
          first part's program (Chessel Duet's guitar played as piano) -
          removed; no single "current" instrument makes sense once a slice
          can hold notes from more than one part. D-5: one MIDI channel per
          part (part-list order, skip channel 10/percussion). SynthEngine
          gained play_chord(events, duration_ms) so a cross-part chord's
          channel groups start together instead of each group's
          stop_all_notes() cutting the previous one off - play_notes() is
          now a thin wrapper around it.

[x] A9  FIX tempo display so it reflects the time signature - reported bug
        > DONE 2026-08-06. music21's getQuarterBPM() always converts to
          quarter-note BPM (Chessel Duet, eighth=96, displayed "48 BPM").
          _extract_tempo now also returns a display_string built from the
          mark's own beat unit ("96 eighth notes per minute"); quarter_bpm
          is kept internally for playback timing, unaffected. UK/US wording
          deferred to F4/D-6.

[x] A10 Extract timeline building from MusicData into
        parsers/timeline_builder.py, one class per file - NFR-03 AC-03.1
        > DONE 2026-08-06. Moved the ElementTree walk out of MusicData
          verbatim (logic unchanged) into TimelineBuilder(file_path,
          parts_info).build(), called from __post_init__. No circular
          import - timeline_builder.py never imports models.music_data.


###
PHASE B - FINISH REGION 2 FILTERING AND UX REWRITE         (5 tasks)   Ref 7
###
[x] B1  MusicData.get_score_structure() emitting the parts_data shape that
        Region2HierarchyModel.build_from_score() documents - Ref 7
        > DONE 2026-08-06. Replaced get_region_2_data(), which built a flat
          dict keyed by f"Staff {s_id}" with no part id - two parts sharing
          a staff number silently overwrote each other's row. This was the
          root cause of the reported "viola has no stave in Region 2" bug on
          Way To Go; Region2HierarchyModel's node ids already included part
          id, so wiring up the real hierarchy fixed it as a side effect.

[x] B2  MusicData.set_active_voice_filter(); filter Region 3, Region 4 and
        playback by the active (part_id, staff, voice) tuples - Ref 7 AC1/AC2
        > DONE 2026-08-06. active_voice_filter: None means unfiltered (not
          "show nothing" - required since most of the existing suite builds
          MusicData without ever calling the setter). New _visible_notes()
          is the single filter point all four accessors read through, so a
          Region 3 row index means the same note everywhere even when some
          notes are hidden. Filtering changes what's shown/played at a
          position, not which positions Left/Right visits.

[x] B3  Remove the hasattr guards in main_window.py; confirm toggling works
        and timeline position is retained - Ref 7 AC3
        > DONE 2026-08-06. Guards removed - get_score_structure()/
          set_active_voice_filter() are unconditional calls now.

[x] B4  Test: toggling the TAB staff off removes the duplicate notes on the
        Bourree sample (first slice is E,G,E,G today) - Refs 7, 8
        > DONE 2026-08-06.

[x] B5  Region 2 UX rewrite: replace the two-column table with a flat list
        navigated by Up/Down only, toggled by O - added beyond the original
        B1-B4 scope after the table view was found "clumsy" and the
        Product Definition Document's Key Strokes table was updated to O
        - Ref 7
        > DONE 2026-08-06. New Region2ListWidget(QListWidget): each row's
          text embeds name/level/status in one line so NVDA reads the whole
          row on one Down-arrow instead of column-by-column table
          navigation, e.g. "Classical Guitar - on", "  Treble stave - on".
          RegionTableWidget stripped back to a plain property table for
          Regions 1/4 only. Confirmed toggling a parent off/on restores each
          child's own prior state (not a reset) - locked in with a test.


###
PHASE C - NAVIGATION AND WINDOW CHROME                    (8 tasks)
###
[x] C1  Confirm/lock in Note-region-scoped navigation - Ref 4, D-2 RESOLVED
        > DONE 2026-08-06. TimelineListWidget already intercepted Left/Right
          scoped to its own focus; confirmed the other three region widgets
          never call navigate_timeline_* and locked it in with a regression
          test.

[x] C2  Ctrl+Left / Ctrl+Right jump by measure - Ref 3
        > DONE 2026-08-06. Jumps are "sympathetic to active notes, not just
          what is in the score" per the user's clarification - skips
          measures left entirely invisible by the Region 2 voice filter.
          Pickup bar (measure 0) falls out for free since measure_numbers()
          already lists it first.

[x] C3  Home / End jump to first and last event - Ref 5
        > DONE 2026-08-06. Unlike Left/Right/Ctrl+Left/Right, Home/End never
          fail outward - no boundary cue call. No screen-reader announcement
          yet (D3).

[x] C4  Type digits then Enter to jump to a bar; Escape clears; unknown bar
        plays an error sound and does not move - Ref 6
        > DONE 2026-08-06. State (_pending_digits) lives in
          TimelineListWidget per D-2 (per-widget, not global).
          jump_to_measure() bounds against _sounding_bounds() so a bar
          that's only trailing rest padding isn't a valid target either. Any
          other handled key clears pending digits first, so stale typed
          digits can't silently wait for a future Enter.
        > FOLLOW-UP 2026-08-06: the user's real 'Long tune' score landed
          (files/long tune/, 130 measures on paper, notes end ~112, rest is
          trailing empty padding - same pattern C5 excludes). Added
          score_long_tune fixture, tested jump to measure 100.

[x] C5  Audible boundary notification ("doh" sound) - Ref 2 AC4, Ref 3 AC4
        > DONE 2026-08-06. D-9: synthesized through the existing SynthEngine
          rather than a new asset - a short, quiet low note on a dedicated
          channel (15) so it can't collide with a part's own instrument.
        > FIX 2026-08-06 (live-tested bug): "active event" for boundary
          purposes included trailing rest-only padding at the end of the
          final bar (Chessel Duet's last bar has rests in every voice on
          beats 4/5/6 after the last real note) - Right/Ctrl+Right stepped
          through the rests before hitting the boundary, and End landed on
          an empty rest instead of the last real note. Fixed by introducing
          _sounding_bounds() (first/last index with a visible note whose
          midi_pitch is not None) as the real navigable range for
          Left/Right/Home/End/measure-jump - an interior rest sandwiched
          between sounding notes stays individually reachable; only
          leading/trailing rest-only padding is excluded.
        > Boundary cue tuned on request: C#2 at 100ms (was E2 at 200ms).

[x] C6  Status bar: measure/beat/key/time-signature at the current cursor
        position, backed by new per-event key+time-signature tracking -
        Functional Spec, resolves the "Status bar" Discussion Point
        > DONE 2026-08-06. Key signature (fifths) now tracked per-EventSlice
          the same way A6 tracks time signature (D-11) - FIFTHS_MAP moved to
          models/key_signatures.py so both Region 1 and the live status bar
          share one mapping without a parsers->models reverse import.
          StatusBarWidget uses QMainWindow.setStatusBar() (not a custom
          widget) so NVDA+End ("report status bar") works natively. Its 3
          fields cycle Tab/Shift+Tab among themselves only, independent of
          the regions' own cycle (see C7).

[x] C7  F6 / Shift+F6 toggle focus between the regions area (last-focused)
        and the status bar - Functional Spec
        > DONE 2026-08-06, revised twice after live testing - see D-10/D-12
          for the settled design. Gotchas found by testing, not assumption:
          1. QMenuBar has no focus policy by default - menuBar().setFocus()
             alone does nothing (moot once the pane count below dropped to
             two, but worth remembering for any future menu-bar focus work).
          2. Qt.ShortcutContext.ApplicationShortcut caused ambiguous-shortcut
             failures once more than one MainWindow existed in the same
             QApplication (true across sequential pytest tests, not just a
             hypothetical) - switched to WindowShortcut, which still fires
             regardless of which child widget within THIS window has focus.
          3. CORRECTION (live-tested, user reversed their own original
             3-pane spec after trying it): F6/Shift+F6 now toggle between
             just TWO panes (regions, status bar) - menu bar access stays on
             the OS's native Alt mechanism. See D-10.
        > FIX 2026-08-06 (live-tested, two rounds): Tab from Region 4 was
          landing on the status bar instead of wrapping to Region 1, and
          fixing that broke a different edge (Region 1 -> status bar instead
          of Region 2). ROOT CAUSE, confirmed by direct experiment:
          QWidget.setTabOrder(a, b) splices into ONE shared window-wide
          focus ring; relocating any node resets its own outgoing pointer as
          a side effect. Closing a 4-widget loop needs all four touched, and
          the dependency between the calls is circular - no call order
          satisfies it. This is a structural limitation of pairwise chain
          splicing applied to a closed ring, not a bug in how this code used
          setTabOrder - not fixable by reordering. Fixed by abandoning
          setTabOrder/focusNextChild/focusPreviousChild for the regions area
          entirely: MainWindow.focus_next_region/focus_previous_region index
          into a fixed 4-widget list and call .setFocus() directly (D-12) -
          same pattern the status bar's own Tab cycling already used.
          GOTCHA WORTH REMEMBERING: don't use setTabOrder to close a
          focus-cycle loop of 3+ widgets in this codebase.

[x] C8  Menu bar: add Navigation menu (First/Last measure, Goto Measure
        dialog) and Help menu (About); Edit/View/Options deferred to their
        owning phases - Functional Spec
        > DONE 2026-08-06. GotoMeasureDialog/AboutDialog are constructed by
          name (not inlined) so tests can monkeypatch main_window's
          reference and inject a pre-built dialog instead of driving a real
          blocking exec() loop. GOTCHA: comparing against
          GotoMeasureDialog.DialogCode.Accepted broke once a test
          monkeypatched the name to a plain function (no DialogCode) -
          compare against the inherited QDialog.DialogCode.Accepted instead.
          GOTCHA: QAction/QMenu objects built as local variables in
          setup_menu() get silently garbage-collected (PySide's Python
          wrapper dies even though the underlying C++ QMenu is still alive
          and parented, producing "libshiboken: Internal C++ object already
          deleted") - every QAction must be kept as a self.* attribute.
        > FOLLOW-UP 2026-08-06 (live-tested refinements): Ctrl+G opens Goto
          Measure pre-filled with the current bar. "Move to First/Last
          Measure" renamed "...Note" to match actual behaviour
          (navigate_timeline_home/end skip trailing rest padding, not
          literal measure 1). Introduced version.py (D-13).
        > FOLLOW-UP 2026-08-07 (live-tested refinements): GotoMeasureDialog's
          setFocus() in __init__ fired before the native window existed, so
          NVDA never caught the focus change until Tab was pressed -
          deferred to showEvent via QTimer.singleShot(0, ...). Reverted
          "Move to First/Last Note" firing from any region - live use showed
          this was exactly the confusing global-navigation behaviour the
          Discussion Point had already ruled out (D-2); both actions are now
          disabled unless Region 3 has focus, and a dedicated always-enabled
          "Move to Notes" (N) answers the "too much Tabbing back" concern
          instead. AboutDialog text split into 3 focusable QLabels so NVDA
          can Tab through it piece by piece. version.py -> 0.1.3.


###
PHASE D - SCREEN READER ANNOUNCEMENTS                     (4 tasks)
###
SKIPPED FOR NOW 2026-08-07: default NVDA behaviour (reading whatever gains
focus) already covers the app adequately - forcing announcements isn't worth
building speculatively. Revisit D1-D4 if a later feature (e.g. phase E
playback) turns out to need a forced/interrupting announcement that focus
changes alone can't provide. Phase E's design below deliberately avoids
depending on D1-D4 - tempo feedback lands as a 4th status bar field (E2)
rather than a forced announcement, and E4-E7 already update the regions via
the existing _update_timeline_views path rather than inventing a new
announcement channel.

[ ] D1  Spike: how to force an NVDA / VoiceOver announcement from PySide6.
        Decide the mechanism and record it as D-3 below - Ref 1 AC4
[ ] D2  Announcement service module; announce region name on region change
        - Ref 1 AC4
[ ] D3  Announce measure number on measure jump and bar goto - Refs 3, 5, 6
[ ] D4  Interrupt in-progress speech when navigation keys are held
        - NFR-05 AC-05.2


###
PHASE E - PLAYBACK ENGINE                                 (10 tasks)
###
PLANNED 2026-08-07 (E1-E7 detailed below; E8-E10 deliberately left as
one-liners - metronome is being left for another session). Three scope
decisions made while planning, all confirmed with the user rather than
assumed:
  - F/S/D and the Space/Ctrl+Space/Shift+Space playback keys are GLOBAL
    (WindowShortcut context, like the existing Ctrl+M/F6), not Note-region-
    scoped like arrow-key timeline navigation. Rationale: D-2 scoped timeline
    navigation to the Note region because those keys mean something else in
    other regions (e.g. Up/Down moving table rows); tempo/transport controls
    have no such collision and are playback-wide settings, not per-row
    navigation.
  - Tempo feedback without Phase D: a 4th status bar field ("Playback tempo:
    X bpm"), not a forced announcement. Reachable via F6/Tab like the
    existing three (C6) - something to read on demand, consistent with the
    Phase D skip above.
  - Multi-tempo scope: MusicData.tempo_bpm stays a single score-wide value
    (today's reality - no sample score has more than one tempo mark). The
    30-300 hard bounds (Ref 12 AC2) are enforced by clamping the OFFSET to
    [30 - tempo_bpm, 300 - tempo_bpm] once at load, not by tracking tempo
    per-position through TimelineBuilder the way time signature/key are
    (A6/C6/D-11). Revisit as its own task if/when a real multi-tempo score
    shows up, same pattern A6/C6 themselves followed.
    > REVISITED 2026-08-07: the user generated "Slow then quick" (files/
      Slow then quick/score.xml, 5 measures, tempo 100 -> 80/140 -> 60) and
      reported playback ignored every marking after the first. Per-position
      tempo tracking built after all - see E1's second FIX note below.

[x] E1  Separate playback tempo from score tempo; enforce 30-300 bpm - Ref 12 AC1/AC2
        > DONE 2026-08-07. playback_tempo_offset clamped once at write time
          to [MIN-tempo_bpm, MAX-tempo_bpm] (single fixed tempo_bpm, so one
          clamp suffices - see the multi-tempo scope decision above).
          tempo_bpm itself (Region 1's display) is never written to.
        > FIX 2026-08-07 (live-tested, Chessel Duet eighth=96): offset and
          the 30-300 clamp were computed in tempo_bpm's own quarter-note-BPM
          units, not the score's own display units (A9) - raising by "10"
          via F silently meant 10 quarter-BPM (20 in the units the user
          actually reads/types). playback_tempo_offset now lives in display
          units; effective_tempo_bpm() (quarter-note BPM, used internally by
          playback) converts back via the beat-unit ratio A9 already
          computed but never exposed numerically. GOTCHA WORTH REMEMBERING:
          any new tempo-facing field must be explicit about which unit
          system it's in (internal quarter-BPM vs the score's own display
          unit) - this bug recurred twice more (E2, E3) from the same
          conflation before it was fully stamped out.
        > FIX 2026-08-07 (second round, real mid-score tempo changes):
          TimelineBuilder now walks every <sound tempo>+<metronome> marking
          in the file, not just the first - MusicData.tempo_changes, queried
          via _tempo_change_at(index) so tempo-reading methods respect
          whichever marking is in effect at the cursor, not just the
          score's opening tempo. <sound tempo="X"> is always quarter-notes-
          per-minute regardless of the marking's own beat unit - only the
          beat-unit label needs translating.

[x] E2  Tempo up/down in 10 bpm steps and a reset control - Ref 12 AC3/AC4
        > DONE 2026-08-07. F/S/D (tempo faster/slower/reset) are GLOBAL
          shortcuts (WindowShortcut), not Note-region-scoped, since tempo is
          a playback-wide setting with no key collision like arrow-key
          navigation has (see the Phase E scope note above). Status bar's
          4th field shows "Playback tempo: X bpm (score default)" when the
          offset is zero, so it's obvious no offset is applied.
        > FIX 2026-08-07: same units bug as E1 - status field showed raw
          quarter-note BPM instead of the score's display units.
        > FOLLOW-UP 2026-08-07: 5th status bar field, "Playback:
          Stopped/Playing/Paused" (Ref 10), added after live testing showed
          no way to check playback state without listening. Phrase audition
          (E6) and pause/resume must NOT trigger a full status bar refresh
          (would disturb the position fields the user specifically praised
          staying put) - new set_field(index, text) updates just one field.

[x] E3  Tempo dialog accepting decimal values - Ref 12 AC5
        > DONE 2026-08-07. This is a TEMPO OFFSET dialog, not an absolute-
          tempo one, per the user's reasoning: the score's own tempo can
          vary by position (multi-tempo, E1's second FIX), and an absolute-
          value dialog would fight that. An out-of-range value is CLAMPED on
          accept, not rejected (matches AC2's "boundaries are enforced").
        > FIX 2026-08-07: same tempo-units bug as E1/E2 (dialog prepopulated
          "82" for an offset the user had set to "130") - no logic change
          needed once E1's fix landed, only the label, which now names the
          actual unit so entered/displayed numbers can't be ambiguous again.
        > FOLLOW-UP: opens on Ctrl+T too, same scope as Ctrl+G.

[x] E4  Sequencer: schedule events over time from a start index, with
        stop/pause/resume. Foundation for E5-E9.
        > DONE 2026-08-07. New EventSlice.quarters_from_start (a running
          total independent of the ts-relative display units) makes real
          elapsed-time scheduling possible. Single QTimer, rescheduled one
          step at a time (not N pre-scheduled timers) so a mid-playback F/S
          tempo change just cancels and reschedules the next step rather
          than unwinding a queue built with stale timing - tempo is re-read
          live on every step. Sequencer takes an injectable `timer`
          constructor argument so tests drive scheduling deterministically
          via a FakeTimer instead of a real wall-clock wait.
          get_playback_events_for_indices()/_visible_notes() gained an
          optional `index` parameter so the Sequencer can read an arbitrary
          slice without disturbing active_event_index/Region 3's selection
          (the update_cursor flag - phrase audition, E6, needs this).
        > FIX 2026-08-07 (live-tested, "pause is not working"): pause() left
          the sounding note ringing out on its own natural duration instead
          of silencing it - for a short note that's under a second either
          way, so there was no audible confirmation pause had done anything.
          pause() now also calls synth.stop_all_notes() immediately.

[x] E5  Play / pause / stop keystrokes; pause refreshes the regions to the
        new position; stop reverts to the original start - Ref 10
        > DONE 2026-08-07. Space/Ctrl+Space are global (playback transport,
          not timeline navigation, so D-2's Note-region scoping doesn't
          apply - same as Ctrl+M). Each Sequencer step sets
          active_event_index and refreshes the regions, which satisfies
          AC4's "pausing refreshes the regions" for free.
        > FIX 2026-08-07: Space on the last active note now plays the
          boundary cue instead of starting playback with nothing ahead to
          play.
        > FIX 2026-08-07 (second round): Space while paused was treated the
          same as Space while playing (stop + revert to original start), so
          resuming after Ctrl+Space needed two Space presses. toggle_play_stop()
          now checks is_paused first and resumes instead. Ctrl+Space
          narrowed to pause-only (it used to also resume on a second press,
          colliding with Space's new job).

[x] E6  Two-bar phrase audition on Enter; re-trigger while playing stops it - Ref 11
        > DONE 2026-08-07. Fills in the "buffer empty" Enter branch C4 left
          inert. update_cursor=False (E4) means active_event_index is
          untouched once the phrase finishes - this is a listen-to-context
          action, not navigation. Enter stops ANY active Sequencer run, not
          just another phrase (symmetric with Space stopping a phrase) - the
          existing update_cursor flag already distinguishes a
          cursor-tracking run from a phrase one, so no separate "is a phrase
          auditioning" flag was needed.

[x] E7  Chord audition on Shift+Space, holding until released or re-pressed - Ref 13
        > DONE 2026-08-07. Per the user's clarification, no keystroke-held
          tracking needed - each note's own duration already satisfies "held
          for its marked length", and "or pressed again" is satisfied by a
          plain re-trigger (SynthEngine.play_chord already stops previously-
          active notes before starting new ones, A8). Needed no Sequencer
          involvement at all - one new global QShortcut.

BUG FIX 2026-08-07 (reported hangover from E4/E5, live-tested): Sequencer
reaching the last note flipped is_playing False and emitted finished()
immediately - before the note had actually rung out - so a Space pressed
right after took the "start a new run" branch instead of "stop", and since
the cursor was already on the last note that played the boundary cue instead
("no man's land"). Fixed by scheduling a genuine ring-out wait
(_pending_next_index left None as the signal this wait is the finish, not a
further step) before flipping is_playing False - applies to both full
playback and phrase audition (E6), same code path.

[x] E8  Metronome: click every beat, accented click on beat 1, tick when
        stepping onto a beat position - Ref 14 AC1-AC3
        > DONE 2026-08-07. Reuses the existing in-process FluidSynth engine
          (confirmed with the user) rather than a second audio backend, to
          stay inside the pipeline already characterised for Ref 9 latency.
          play_click() is fully independent of the melodic note pipeline -
          doesn't call stop_all_notes() (so it doesn't cut off a note
          sounding at the same beat) and isn't touched by a later
          play_chord() (so it rings its own fixed duration regardless of the
          next note's length).
        > FIX 2026-08-07 (live-tested, "was awful"): swapped the synthesized
          sawtooth lead for GM percussion Claves. GOTCHA: GM percussion only
          maps correctly on MIDI channel 10 (0-indexed 9), so
          METRONOME_CHANNEL moved there from an arbitrary 14 - not a
          collision with a score's own percussion part, since multiple
          percussion notes legitimately sound at once on channel 10 same as
          a real drum kit. Percussion notes have no tunable pitch, so beat
          1's accent is now velocity (127 vs 100), not a different note
          number. See D-14 - still not a satisfying sound (E11 open).

[x] E9  With metronome on, beat positions become navigable events even where
        no note sounds - Ref 14 AC4, depends on A4
        > DONE 2026-08-07. IMPORTANT CORRECTION mid-task: the first cut
          merged synthetic beat markers directly into timeline_slices
          unconditionally at parse time ("cheap, harmless when metronome is
          off") - broke 9 existing tests, because CLAUDE.md's documented
          Timeline model ("one entry per distinct (measure, offset) WITH AT
          LEAST ONE SOUNDING NOTE") is relied on by direct-indexing tests
          throughout the suite. Fixed properly: beat_markers is kept as a
          SEPARATE list; set_metronome_enabled() only merges it into
          timeline_slices when turning ON and restores the untouched real
          list when turning OFF, relocating active_event_index to the
          nearest equivalent real position across the rebuild.
          _slice_has_visible_sounding_note/_sounding_bounds deliberately
          UNCHANGED - the outer Left/Right/Home/End boundary of the piece
          stays anchored to real sounding notes only, so metronome mode
          can't resurrect trailing rest-only padding as navigable (though
          the Sequencer's own playback walk WILL click through trailing
          padding during a full play-through - consistent with AC1 having
          no such exclusion). GOTCHA WORTH REMEMBERING: never merge
          synthetic/derived slices into timeline_slices unconditionally -
          plenty of code assumes that list is exactly the real, sounding
          timeline.

[x] E10 Latency harness; verify audition stays within 25 ms - Ref 9 AC3, NFR-04
        > DONE 2026-08-07. New latency_harness.py at the repo root, not
          tests/ - it needs a real SynthEngine, which tests/conftest.py's
          autouse fixture deliberately blocks (D-7); run by hand via
          `.venv\Scripts\python.exe latency_harness.py`, no pass/fail
          assertions, just a printed report (no hardware-backed CI to assert
          against). Measures per-note dispatch latency and Sequencer
          scheduling jitter. Verified live: play_notes mean 0.023ms/max
          0.341ms, play_click mean 0.012ms/max 0.018ms (both far under the
          25ms budget); Sequencer jitter -3.4ms to +0.6ms at 240bpm.

[ ] E11 Metronome click sound quality - functional debt, not a missing
        feature (Ref 14 AC1-AC3 are all functionally met by E8) - see D-14.
        > OPENED 2026-08-07. Two synthesis attempts, both live-tested by the
          user and found lacking: a synthesized sawtooth lead ("awful"),
          then GM percussion Claves ("better but still not really good
          enough"). Next step per the user's own original framing when E8
          was planned: probably the raw-waveform-or-WAV-asset route
          (audio/synth_engine.py's SynthEngine is FluidSynth/soundfont-only
          - no built-in tone generator - so an exact-spec click, e.g. a
          precise 1200Hz/800Hz sawtooth, needs either a small procedurally-
          generated PCM path alongside FluidSynth's WASAPI stream, or two
          short user-supplied WAV clips) - deliberately not attempted yet;
          revisit when picked up, including checking whether a second
          concurrent audio stream competes with FluidSynth's own
          low-latency WASAPI session (Ref 9).


###
PHASE F - NOTE DISPLAY CONFIGURATION                      (4 tasks)
###
[x] F1  Formatting layer building the Region 3 label from a configurable
        attribute list, e.g. "C, string 5, fret 1" - Ref 15
        > DONE 2026-08-07. Two gaps against the literal PDD text, resolved
          with the user rather than assumed: Ref 15 AC4 names a 4th "score"
          scope beyond voice/stave/part (kept even though Ref 27 AC2 only
          mentions three); a stave/part/score-scope action fired from a
          multi-note (chord) selection unions the scope across every
          selected note, not just the one the menu was opened on.
          voice_display_attributes: Dict[(part_id, staff, voice), Set[str]],
          defaulting to {"step"} (today's plain-name behaviour) - "step" is
          itself toggleable off, rendering a blank but still navigable row.
          Region4TableWidget uses Qt's CustomContextMenu policy, assumed (at
          the time) to raise customContextMenuRequested for both
          right-click and the Menu/Application key - see the first FIX
          below, that assumption was wrong.
        > FIX 2026-08-07 (live-tested - menu unreachable by keyboard):
          ROOT CAUSE, confirmed empirically: Qt does NOT synthesize
          customContextMenuRequested from Menu/Shift+F10 for a QTableWidget
          - only a real right-click reliably raises it. GOTCHA WORTH
          REMEMBERING for any future CustomContextMenu-policy widget in this
          app. Region4TableWidget.keyPressEvent now handles
          Key_Menu/Shift+F10 explicitly, calling the same
          _show_attribute_menu path a right-click uses. Also: row/position
          resolution now uses rowAt(pos) for a real click and only falls
          back to currentRow() for the keyboard path, since a right-click on
          a different row than the keyboard-focused one should act on the
          row actually clicked.
        > FIX 2026-08-07 (live-tested - "Unknown display-attribute scope:
          False" traceback): the REAL reason no attribute was ever added, in
          every scope. ROOT CAUSE: QAction.triggered always calls its
          connected slot with one positional bool ("checked"). The per-scope
          menu callback was `lambda scope=scope: ...` - Qt/PySide calls a
          connected Python callable with one positional arg whenever it
          accepts one, so that bool landed in scope's own default-arg slot
          instead of "voice"/"stave"/etc. Invisible to this session's own
          tests, which all called the callback with zero arguments - never
          exercising the argument a real QAction actually passes. Fix:
          lambda now takes `checked=False, scope=scope` - checked soaks up
          the bool, scope keeps its loop default. GOTCHA WORTH REMEMBERING:
          any QAction.triggered-connected lambda in this codebase needs a
          leading throwaway positional/keyword arg, or it silently
          misassigns whatever real argument comes first - and a test that
          calls such a callback with zero arguments cannot catch this class
          of bug.
        > FIX 2026-08-07 (two more findings):
          1. FIXED - no NVDA announcement on Application-key press, only
             after arrowing once. menu.exec(global_pos) was called with no
             `at` argument, so nothing was pre-highlighted. GOTCHA: QMenu.exec
             is NOT reliably monkeypatchable in this PySide6 build - a test
             that tried `monkeypatch.setattr(QMenu, "exec", fake_exec)`
             silently fell through to the real blocking call and hung the
             suite (confirmed twice). Worked around by splitting menu
             construction from the exec() call so tests can assert against
             the built-but-not-exec'd menu instead.
          2. A "attribute not actually added" report could not be
             reproduced this round - turned out to be a stale running
             process, confirmed in the next FIX entry.
        > FIX 2026-08-07: confirmed the "not added" report was the user
          testing a stale process (old code still running), not a real bug.
          Also confirmed Region 3's attribute rendering order is ALWAYS the
          fixed DISPLAY_ATTRIBUTE_ORDER regardless of which scope an
          attribute was added through (real ordering control is F2, still
          open). Two more menu-interaction bugs:
          1. FIXED, high confidence - ROOT CAUSE: selecting a menu action
             rebuilds Region 4's rows WHILE the menu's own exec() is still
             running (QAction.triggered fires before exec() returns), which
             reset the table's current cell - so Enter (which triggers a
             rebuild) lost the row, while Escape (no rebuild) correctly kept
             it. Fixed with _restore_region_4_focus_after_menu(row, column),
             called after exec() returns.
          2. BEST-EFFORT, reverted next round (see below) - tried
             pre-highlighting the first menu item via exec(pos, at=) plus a
             synthetic Key_Down QKeyEvent, to get an immediate NVDA
             announcement without a manual arrow press.
        > FIX 2026-08-07 (confirms the focus-restoration fix works):
          1. FIXED - opening the menu from Region 4's VALUE column returned
             focus to the KEY column - the column was hard-coded to 0 in two
             places; both now thread the actual currentColumn() through.
          2. REVERTED - the prior round's synthetic-announcement attempt
             (exec(pos, at=first_action) + synthetic Key_Down via
             QApplication.sendEvent) did NOT work: live-tested, the
             Application key announced "Python window" and NVDA+Up reported
             "blank" instead of the item text - something was genuinely
             highlighted (the user's next Down landed on the SECOND item)
             but carried no real accessible name. GOTCHA WORTH REMEMBERING:
             Qt's accessibility bridge only hands UIA/MSAA focus down to a
             menu-item sub-object on a GENUINE keyboard-navigation event,
             not a programmatic setActiveAction/exec(at=)/sendEvent one -
             don't re-attempt this without a way to verify against real
             NVDA. Reverted to a plain menu.exec(global_pos) (one manual
             arrow press needed - "more normal" anyway, per the user).

[ ] F2  Options dialog to control note-display attribute ORDERING - Ref 15
        AC4. Scope control (global/part/staff/voice - now voice/stave/part/
        score) shipped with F1 above via Region 4's context menu instead of
        a separate Options dialog; only the ordering half remains open.
[x] F3  Capture further attributes from the source file: dynamics, staccato,
        fingering - Ref 16 AC3
        > DONE 2026-08-10, live-tested against a real MuseScore export
          ("Fingers and articulations"). NoteData gained dynamic/
          articulation/fingering/pluck (Optional[str]) - TimelineBuilder
          parses notations/articulations + notations/ornaments (merged into
          one comma-joined "articulation" field), notations/technical/
          fingering, and notations/technical/pluck (guitar RH p/i/m/a).
          Dynamics are trickier: MuseScore emits a <direction> sibling of
          <note>, not a child of it, so a per-measure pending_dynamics dict
          keyed by (staff, offset_divs) matches the mark to whichever
          note(s) land at that same offset - chord notes share the base
          note's offset so they inherit it for free, no special-casing
          needed. notations/dynamics directly on a note (rarer) overrides
          the direction-based match when present. New models/vocabulary.py
          dynamic_name()/articulation_name() give spoken forms (f->forte,
          strong-accent->marcato, trill-mark->trill...), falling back to raw
          text for anything unmapped.
          No Region 3/4 UI work was needed at all - F1's existing
          voice_display_attributes/DISPLAY_ATTRIBUTE_ORDER/
          _note_attribute_pairs machinery (Ref 15 AC4) already shows a key
          only when it's both toggled on for that voice AND present on the
          note, so the four new keys just slot into DISPLAY_ATTRIBUTE_ORDER
          and _note_attribute_pairs and the context menu/scope toggles work
          unmodified.
          Piano LH/RH hand is deliberately NOT inferred from staff/clef -
          MusicXML's <fingering> carries no hand flag, so both hands render
          under one generic "fingering" key and the existing "stave"
          attribute supplies hand context when toggled on alongside it
          (user-confirmed choice over guessing from clef).
        > FIX (live-tested against the real file, two findings):
          1. notations/technical can carry MORE THAN ONE <fingering>/
             <pluck> on a single note (MuseScore's rasgueado marking: one
             note, three <pluck> children p/i/m/a together). The original
             code used ElementTree .find(), which only returns the first
             match - silently dropped the rest. Switched to .findall() +
             ", "-join for both fields. Regression fixture
             tests/fixtures/multi_value_technical.musicxml added.
          2. NVDA didn't announce a slice's note automatically when Left/
             Right navigation landed on exactly ONE note - only Up/Down
             worked. ROOT CAUSE: _update_timeline_views' rebuild calls
             region_3.clear() (resets the view's "current" item to none)
             then selectAll() (marks rows selected but deliberately leaves
             "current" untouched) - so after a rebuild there was never a
             definite current/focused item for the accessibility bridge to
             point at. With several notes selected, NVDA still gets a
             selection-changed announcement; with exactly one, nothing told
             it which item that was. Fixed by adding
             region_3.setCurrentRow(0) after blockSignals(False) (so the
             selection model's real currentChanged signal fires) - the
             1-arg form uses QItemSelectionModel::NoUpdate so it doesn't
             disturb the selectAll() selection. This is a general Region 3
             fix, not F3-specific (any rebuild landing on a single-note
             slice had the same gap). Regression test
             test_navigating_onto_a_single_note_slice_sets_a_current_row
             added, verified to fail without the fix via git stash.
[x] F4  UK / US music vocabulary toggle: bar vs measure, crotchet vs quarter
        note, quaver vs eighth note - Discussion Points, see D-6
        > DONE 2026-08-10. Purely presentational per the user - internal
          identifiers (attribute_key "measure", method names, MusicXML type
          strings) never change, only rendered text. New models/vocabulary.py
          (Qt-free) holds the word-choice/translation helpers; MusicData
          gained uk_terms: bool (default False - required for backward
          compatibility with every existing test's US-dialect assertions).
        > Default dialect: UK, unless QLocale.system().territory() resolves
          confidently to United States, in which case US - any other/
          indeterminate locale stays UK (never a guess). Lives in
          main_window.py, not models/, since it needs Qt; injectable via a
          new MainWindow(uk_terms=...) constructor param (same D-7 DI
          pattern as synth) so tests get a deterministic result regardless
          of the machine running them - GOTCHA hit live: this dev machine's
          own OS locale is en_GB, so the existing test suite (previously
          assuming US wording unconditionally) broke wholesale until the
          window fixture pinned uk_terms=False explicitly.
        > uk_terms is a SESSION preference, not a per-score one (unlike
          metronome_enabled/voice_display_attributes) - MusicData is wholly
          replaced on every file load, so _on_score_loaded explicitly
          reapplies MainWindow's own self._uk_terms onto each freshly-loaded
          MusicData, and set_uk_terms() (unlike toggle_metronome) still
          works with no score loaded. Region 1's "Tempo" credit is baked
          once at parse time in US units (MusicXMLReader) - get_region_1_data
          now overrides it live from tempo_bpm/tempo_beat_unit_quarter_length
          directly rather than returning self.credits verbatim, so toggling
          is reflected without reloading the file.
        > FIX 2026-08-10 (live-tested, three findings):
          1. Menu shape wrong - a single checkable "UK Terms" action doesn't
             show "at least one option is always active" as clearly as the
             user wanted. Replaced with Options > Terminology Language, a
             submenu of two mutually exclusive checkable items (UK/US) via
             QActionGroup(exclusive=True) - clicking one always leaves
             exactly one checked, satisfying "must have at least one ticked"
             structurally rather than by convention. toggle_uk_terms()
             replaced by set_uk_terms(bool), called from two thin
             _select_uk_terms/_select_us_terms wrappers (the usual
             QAction.triggered(bool) gotcha - see F1's postmortem - handled
             by giving each its own named callback instead of a scope=scope
             lambda).
          2. Region 1/4's current cell was jumping to the top-left cell on
             every terminology-language change. ROOT CAUSE: _populate_table
             (the shared Region 1/4 rebuild helper) calls clearContents()/
             setRowCount(), which resets QTableWidget's current cell to
             (0, 0) - true for ANY refresh through this method, not just the
             F4 toggle. Fixed at the source: _populate_table now captures
             current row/column before rebuilding and restores them after
             (clamped to the rebuilt table's bounds), so this is now a
             general Region 1/4 position-persistence guarantee, not
             something F4-specific. Made
             test_restore_region_4_focus_after_menu_...'s own "sanity check:
             the rebuild really did reset it" assertion obsolete (that
             method's remaining job is now specifically returning WIDGET
             FOCUS after a QMenu.exec(), not the cell position - both used
             to be conflated in one bug).
          3. D-15 opened and resolved live - see below: stave/staff excluded
             from the toggle entirely (models/vocabulary.py's staff_word/
             clef_name removed again), Region 2/4's clef-name wording is
             unaffected by uk_terms.


###
PHASE G - PER-SCORE PERSISTENCE                           (2 tasks)   Ref 27
###
[ ] G1  Persist part/staff/voice toggles and metronome state per score file
        - Ref 27 AC1
[ ] G2  Persist note display configuration per scope - Ref 27 AC2


###
PHASE H - TABLATURE                                       (4 tasks)
###
[ ] H1  Left and right hand finger attributes on notes - Ref 20
[ ] H2  Capo: raise playback pitch, show note names and frets relative to it - Ref 21
[ ] H3  Identify the chord sounding at the cursor and name it - Ref 22 AC1/AC3
[ ] H4  Describe fretboard shapes in words, e.g. "barred G at 3rd fret" - Ref 22 AC2


###
PHASE I - ROBUSTNESS AND ERROR HANDLING                   (2 tasks)
###
[ ] I1  Accessible error dialog for corrupt or unreadable files, replacing the
        [ERROR] prints in main_window.py:132 and music_data.py:31
        - Ref 25 AC3, NFR-06 AC-06.1
[ ] I2  Preserve unhandled custom MusicXML tags through save and export
        - NFR-06 AC-06.2


###
PHASE J - FILE I/O                                        (6 tasks)
###
[ ] J1  Import MIDI, types 1 and 2 - Ref 25
[ ] J2  Import Guitar Pro (needs PyGuitarPro) - Ref 25
[ ] J3  Import Braille Music Editor - Ref 25
[ ] J4  Export MusicXML, preserving bars, notes, string and fret data - Ref 26
[ ] J5  Export MIDI - Ref 26
[ ] J6  Export Guitar Pro and Braille Music Editor - Ref 26


###
PHASE K - EDITING MODES                                   (2 tasks)
###
[ ] K1  Ctrl+Shift+E toggles Read-Only / Edit mode with an announcement;
        editing controls locked in Read-Only - Ref 23
[ ] K2  Edit note attributes by typing over a value or stepping it; play the
        updated note immediately - Ref 24


###
PHASE L - VOICE CONTROL                                   (3 tasks)   Ref 19
###
[ ] L1  Spike: Windows SAPI 5.4 / Windows.Media.SpeechRecognition - Ref 19
[ ] L2  Command grammar (Tab, Next, Previous, Next Bar, Play Bar...) plus the
        Ctrl+Shift+Enter toggle - Ref 19
[ ] L3  macOS speech recognition equivalent - Ref 19, see D-4


###
PHASE M - CROSS-PLATFORM AND PACKAGING                    (4 tasks)
###
[ ] M1  macOS audio driver selection (coreaudio in place of wasapi) and
        keyboard/focus parity with Windows - NFR-01
[ ] M2  Windows installer (.exe / .msi) with bundled runtime - NFR-02 AC-02.1/AC-02.3
[ ] M3  macOS signed .dmg or .app bundle - NFR-02 AC-02.2
[ ] M4  Audit for zero network calls in core features - NFR-07


###
OPEN DESIGN DECISIONS
Mirrors the Discussion Points table in the Product Definition Document.
Add to this as decisions come up; move the outcome into the spec once settled.
###

D-1  Rests in one voice alongside sounding notes in another
     Decided 2026-08-04: an explicit rest in the score is an event with a
     step of "rest" and shows as "rest" in the Note region.
     RESOLVED 2026-08-06 in B2: no special-casing needed. Rests carry a real
     (part_id, staff, voice) since A4/A5, exactly like sounding notes, so
     MusicData._visible_notes() filters a rest the same way it filters any
     other note - a rest in a soloed/active voice shows as "rest" in Region
     3, a rest in a filtered-out voice does not appear at all.

D-2  Global navigation keystrokes vs per-widget handling
     DECIDED 2026-08-06: per-widget, scoped to the Note region, not global.
     Already flagged in the spec's own Discussion Points; the user reversed
     the original lean toward global handling - too confusing and not
     standard Windows behaviour to have arrow keys act application-wide when
     they mean something else in other regions. Ref 4 updated accordingly.
     TimelineListWidget.keyPressEvent already handles Left/Right this way;
     C1/C2/C3 extend the same widget rather than adding a MainWindow-level
     event filter or WindowShortcut context. Revisit only if Tab-ping-ponging
     back to the Note region proves inefficient in practice.

D-3  Mechanism for forcing screen reader announcements from PySide6
     Needed for Ref 1 AC4, Ref 3 AC3 and Ref 5. Candidates: QAccessible
     alert events, live-region style updates, or driving the screen reader
     API directly. Decide in D1.

D-4  macOS speech recognition API
     Listed as TBC in the spec. Windows path is SAPI 5.4 /
     Windows.Media.SpeechRecognition. Decide in L3.

D-5  Multi-part GM programs and MIDI channel allocation
     DECIDED 2026-08-06 in A8: one MIDI channel per part (not per staff -
     staves within a part share an instrument in every sample score, and
     per-staff allocation would burn through the 16-channel ceiling faster
     for no observed benefit), assigned in part-list order, skipping
     channel 10 (index 9, percussion), wrapping past 16 parts.
     MusicData.get_channel_for_part() / get_gmidi_program_for_part().

D-6  UK vs US music vocabulary
     RESOLVED 2026-08-10 in F4. Default: UK, unless QLocale.system() resolves
     confidently to a US territory - any other/indeterminate locale stays
     UK. Screen reader announcements: no separate channel needed (Phase D
     was already skipped) - NVDA reads whatever on-screen text the toggle
     produces, so on-screen and spoken wording are automatically the same
     thing. Region 4 attribute names: the internal "measure" key is NEVER
     renamed (storage/menu wiring depend on it staying stable) - only the
     rendered display LABEL changes, via models/vocabulary.attribute_label().
     Surfaces via Options > Terminology Language (a UK/US submenu, not a
     single toggle - see F4's live-tested FIX notes). Scope: bar/measure and
     the duration-name family (crotchet/quaver/etc) only - stave/staff was
     tried and then deliberately excluded, see D-15.

D-7  How tests avoid opening audio hardware
     DECIDED 2026-08-05: constructor injection. MainWindow(synth=None)
     defaults to a real SynthEngine but accepts a stand-in, so tests can
     assert which notes, channel and program would have played - needed by
     Refs 8, 9, 13 and 14. Also makes the engine swappable for the macOS
     driver work in NFR-01.

D-8  Where test sample scores live
     DECIDED 2026-08-05: both. files/ is now tracked in git (66 KB) so the
     real MuseScore exports are backed up and usable as integration
     fixtures, plus hand-written minimal MusicXML under tests/fixtures/ for
     unit tests that need to isolate one behaviour.

D-9  Source of the boundary ("doh") sound - Ref 2 AC4, Ref 3 AC4, see C5
     DECIDED 2026-08-06 in C5: synthesize through the existing SynthEngine -
     a short (quaver-length, ~200ms), quiet, low double-bass note (E2, GM 44
     Contrabass), not tied to the score's own tempo, on a dedicated channel
     (15) reserved from D-5's per-part allocation. No new asset, no
     licensing question (never was going for a literal Homer Simpson
     sample - just a low, unobtrusive indicator that a boundary was hit).
     Making it configurable on/off, and choosing a different instrument/
     pitch, is explicitly deferred until it's actually asked for.

D-10 F6 pane-cycling mechanism and scope - Functional Spec, see C7
     DECIDED 2026-08-06, REVISED 2026-08-06 after live testing: originally
     three panes (menu bar, regions, status bar) - the user's own initial
     spec - but after trying it, the user reversed that: F6/Shift+F6 now
     toggle between just TWO panes, the regions area and the status bar.
     Menu bar access is left to the OS's native Alt mechanism, not F6,
     matching ordinary Windows application behaviour instead of adding a
     bespoke path to something already reachable natively. Mechanism: a
     WindowShortcut-context QShortcut on MainWindow (not
     ApplicationShortcut - see C7's DONE note for why that caused ambiguous-
     shortcut failures) so it fires no matter which child widget holds
     focus. Tab/Shift+Tab keep their existing job of cycling the four
     regions (and, new in C6, cycling a pane's own internal fields for the
     status bar) and stay out of pane-switching entirely. The "regions" stop
     restores whichever region was last focused (tracked via
     QApplication.focusChanged), defaulting to Region 1 before anything has
     been focused - this is a partial answer to the "Focus memory in
     regions" Discussion Point: it remembers the region, not the row within
     it. Since a two-pane toggle has no distinct "reverse", both F6 and
     Shift+F6 call the same _toggle_pane().

D-11 Status bar field tracking and FIFTHS_MAP relocation - Functional Spec,
     see C6
     DECIDED 2026-08-06: key signature (fifths) becomes per-EventSlice state,
     tracked through the score exactly the way A6 already tracks time
     signature (both can change mid-piece, see score_way_to_go) - not a
     separate lookup, not read once at file-open like Region 1's summary
     values. FIFTHS_MAP moves from parsers/musicXML_reader.py to new
     models/key_signatures.py so models/music_data.py can read the current
     position's key without a parsers->models reverse import; Region 1's
     display and the status bar's live display now share one mapping. Status
     bar key wording keeps the existing "/" format ("G major / E minor"),
     not the alternate "or" wording floated during planning - no wording
     change, everywhere stays consistent.

D-12 Region cycle (Tab/Shift+Tab within the MAA) mechanism - Functional
     Spec, see C1/C7
     DECIDED 2026-08-06, after a second live-tested Tab bug (see C7's second
     FIX note): QWidget.setTabOrder cannot reliably close a 4-widget loop at
     all here - not a matter of getting the call order right, a structural
     limitation (relocating any one of the four widgets to fix one edge
     necessarily breaks another, and the four required relocations form a
     genuine cycle of dependencies). Region cycling now bypasses Qt's global
     focus chain entirely: MainWindow.focus_next_region/focus_previous_region
     index into a fixed 4-widget list and call .setFocus() directly, and
     RegionTableWidget/Region2ListWidget/TimelineListWidget's Tab/Backtab
     handlers call those instead of window().focusNextChild()/
     focusPreviousChild(). This is the same pattern C6's status bar fields
     already used for their own internal Tab cycling (StatusBarWidget.
     focus_field(), never routed through the global chain) - now applied
     consistently to the regions area too. No more QWidget.setTabOrder calls
     anywhere in the app.

D-13 Version numbering - Functional Spec, see C8 (About dialog)
     DECIDED 2026-08-06: semantic-ish X.Y.Z in version.py, starting at
     0.1.1, shown in the About dialog. The user owns Y (minor) and X
     (major) bumps and will say when those change; Z (patch) is bumped by
     whoever commits+pushes, on every push. Nothing in the codebase
     automates the bump (no git hook) - it's a manual step to remember when
     asked to commit and push.

D-14 Metronome click sound synthesis - Ref 14, see E8/E11
     OPEN 2026-08-07: not decided, deliberately parked as functional debt.
     Two attempts tried and rejected live: (1) a synthesized sawtooth wave
     through the existing FluidSynth pipeline (GM "Lead 2", tuned to the
     nearest MIDI notes to the originally-requested 1200Hz/800Hz) - "was
     awful"; (2) GM percussion Claves on channel 10, accent via velocity
     instead of pitch (a percussion voice has no tunable pitch) - "better
     but still not really good enough". Both stayed inside the existing
     in-process FluidSynth engine specifically to avoid opening a second
     concurrent audio stream of uncertain latency alongside its low-latency
     WASAPI session (Ref 9) - that tradeoff needs revisiting if soundfont-
     based synthesis genuinely can't be made to sound right, since the
     remaining options (procedurally generated raw PCM, or user-supplied
     WAV clips - see the original click-generation discussion this session)
     both require a second audio path. See E11 for the open task.

D-15 Stave/staff excluded from F4's UK/US vocabulary toggle
     DECIDED 2026-08-10, live-tested: F4 originally translated stave/staff
     too (Region 2's clef names, Region 4's "stave" attribute), on the
     user's own initial instruction to treat every UK/US term pair the same
     way. After trying it live, the user reversed that: Region 2's clef
     names ("Treble stave", "Bass stave" on Chessel Duet) are the app's own
     generated labels (from detecting the MusicXML clef sign - MusicXML
     itself has no English "stave"/"staff" text, it's <clef><sign>G</sign>),
     not literally read from the file - but the user's underlying call is
     independent of that clarification: keep it simple for now, don't
     translate this family at all, regardless of the terminology-language
     setting. models/vocabulary.py's staff_word()/clef_name() were removed;
     attribute_label("stave", ...) always passes through unchanged, same as
     any other never-translated attribute key. Revisit only if asked for
     again - the removed functions are recoverable via git log on F4's
     commit if so.


###
COVERAGE CHECK - every requirement appears at least once
  Ref  1 D2        Ref  8 A8,B4     Ref 15 F1,F2     Ref 22 H3,H4
  Ref  2 C5        Ref  9 A8,E10    Ref 16 A5,F3     Ref 23 K1
  Ref  3 C2,D3     Ref 10 E5        Ref 17 A3        Ref 24 K2
  Ref  4 C1        Ref 11 E6        Ref 18 A2,A6,A9  Ref 25 I1,J1,J2,J3
  Ref  5 C3,D3     Ref 12 E1,E2,E3  Ref 19 L1,L2,L3  Ref 26 J4,J5,J6
  Ref  6 C4,D3     Ref 13 E7        Ref 20 H1        Ref 27 G1,G2
  Ref  7 B1-B5     Ref 14 E8,E9,E11 Ref 21 H2

  NFR-01 M1     NFR-02 M2,M3     NFR-03 A1,A2,A10    NFR-04 E10
  NFR-05 D4     NFR-06 I1,I2     NFR-07 M4

  Reported bugs: tempo units A9, playback instrument A8
  Functional debt: metronome click sound quality E8/E11, see D-14
  Discussion Points: nav keys scoped to Note region (RESOLVED) D-2/C1,
                     UK-US vocabulary (RESOLVED) D-6/F4
###
